# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Units 1–11 complete. Unit 2 core flow verified working by user (a few minor checklist items still outstanding — see below). Unit 3 fully verified by user, including the RLS cross-learner test. Unit 4 fully verified by user against the spec's complete checklist. Unit 5 fully verified by user, including the migration/Storage apply, the cross-learner Storage RLS test, and the full in-browser checklist. Unit 6 fully verified by user, including the migration apply, RLS direct test, in-browser feedback check, and Langfuse trace check. Unit 7 fully verified by user, including the migration apply, a real triggered run, the mid-job kill/restart durability test, live chat UI status updates, and the cross-learner Realtime RLS test. Unit 8 fully verified by user against the spec's complete checklist, including the migration apply, RLS direct test, and full live click-through. Unit 9 fully verified by user against the spec's complete checklist, including the migration apply, RLS direct test on all four new tables, and full live click-through. Unit 10 fully verified by user against the spec's complete checklist, including the migration apply, RLS direct test, and live document generation/rendering check for both doc types. Unit 11 fully verified by user against the spec's complete checklist, including the migration apply, `anomaly_templates` seed data, RLS direct test on the new/changed tables, and the full live click-through (out-of-order multi-part timing, window-expiry behavior, qualitative scoring output, live status checklist UI).

## Current Goal

**Direction change (2026-08-19).** The user shared the full record of the live pilot training program this product is modeled on (Teams group chat: diagnostic → targeted batches → explain/review/judge → capstone → graduation to live client work, run with a real trainee), plus the actual training content: `BlossomRetail_Variant_A` pack (Opening TB, Sales Register, Purchase Register, Bank Statement xlsx), `Karbon_VA_House_Practices_Rulebook_v0.2.docx`, and 8 training-module docs (in the user's Downloads folder). Three decisions confirmed with the user:

1. Diagnostic (and later capstone) switch to **authored file packs with authored answer keys**; LLM generation remains for adaptive drills between batches.
2. **Free-form chat Q&A** becomes a core feature (grounded in Rulebook + module docs + current exercise, never the answer key).
3. The group chats + pack folder are the complete context (meeting transcript was truncated; user confirmed nothing further needed from it).

`project-overview.md` and `architecture.md` updated 2026-08-19 to reflect this. Unit 13 (AIA transition) is deferred; new unit order proposed:

- **Unit 13R — Rulebook + module-doc grounding**: extract text from the real .docx files into `/lib/llm/grounding/`, replace `RULEBOOK_GROUNDING_PLACEHOLDER` in coaching/hint prompts. Small, unblocks everything else.
- **Unit 14R — Pack-based diagnostic**: seed BlossomRetail Variant A (B later) files to Storage + authored answer key; diagnostic flow serves the pack as download cards instead of calling exercise generation. **Open item: no answer key exists in the pack folder — it must be authored** (derived from the registers + bank statement + Rulebook) and reviewed by the user before seeding; this is real accounting content work, not just code.
- **Unit 15R — Free-form Q&A in chat**: composer accepts free text anytime; new `qa` call type + schema, grounded per architecture.md.
- AIA transition and capstone re-slot after these.

## Session log — 2026-09-02 (later 2): OVERDRAWN-TILL RECOVERY — no regeneration for the live interns

User decision: Praveen has already completed his current batch in Tally (not yet submitted) and Garima is mid-way; neither batch is to be regenerated. Both posted the impossible deposits, so their books now carry NEGATIVE cash (Garima −20,100; Praveen −55,100 once his key is double-entry). Since the company is one continuous set of books, their next batch must start from that position rather than pretend otherwise.

- **Generator (code):** when the carried-forward cash is negative, the adaptive prompt now states the overdraft and REQUIRES transaction 1 to be a bank→Cash Contra withdrawal of at least the shortfall plus a working float, before any other cash movement — a legitimate "replenish the till" entry, one easy transaction, no rework. `checkCashFeasibility` already enforced it mechanically (any non-restoring tx1 leaves cash negative → rejected); its message now distinguishes "the till OPENS overdrawn, replenish first" from "this transaction drove cash negative". Robust to either learner having skipped the bad deposit instead (extra cash never breaks postability). 4 new tests (203 total), tsc/lint clean.
- **Current keys (SQL, Downloads/patch-current-batches.sql, originals backed up as BACKUP_{garima,praveen}_current_answerkey.json):** both get the 31 carried-forward opening balances so the tie-out is fair on submission (the keys predate fix #1); Praveen's 12 single-leg transactions get their counterpart legs (Cash/bank for contras, receipt/payment bank legs, Sales/Purchases net of tax per the GST-as-metadata model — e.g. Deccan 69,620 gross → Purchases 59,000) so both sides are scored and cash nets truthfully. Added legs are flagged requires_source_document=false (their documents already exist).
- Verified pre-run: a perfect submission against each patched key scores 100% / tie_out true / pass and passes checkDoubleEntry.

Sequence for the user: run the SQL → deploy → interns submit current batch → Level 3 auto-generates opening with the replenishment.

## Session log — 2026-09-02 (later): ONE CONTINUOUS SET OF BOOKS — carried-forward openings, double-entry integrity, tie-out matcher

A full audit (every exercise, every learner, code + DB) surfaced two live bugs and, while fixing them, a third that had been silent since the tie-out was written.

**(1) Adaptive batches could never pass.** Generated answer keys carried no `opening_balances`, so `checkTrialBalanceTieOut` expected closing = that batch's movements alone, while a learner's real Tally export is CUMULATIVE (the company never resets). Proved with a synthetic flawless May submission: 100% score, `tb_tie_out` false, capped at 'partial'. Every adaptive/explain batch was affected; only the diagnostic (which ships its own openings) tied out. Fix: `getExpectedOpeningBalances` (company.ts) nets every prior answer key — openings + entries, GST/TDS excluded since those ride as metadata and are tie-out-exempt anyway — and generateAdaptiveExercise stamps the result onto the batch's answer key before persisting, so expected closing = carried-forward position + this batch. Same flawless submission now scores 100% / tie_out true / **pass**.

**(2) Single-leg answer keys.** Praveen's delivered batch had 12/12 transactions with ONE leg ("Dr HDFC Bank 90,000", no credit): the missing side was never scored, TB maths counted half of each transaction, and checkCashFeasibility was blind to the cash legs — which is why his unpostable ₹90,000 deposit passed the cash walk. New `checkDoubleEntry` (generate-exercise.ts) requires ≥1 Dr and ≥1 Cr leg per transaction and, for transactions with no tax metadata, exact Dr/Cr balance. Deliberately tolerant of the documented GST-as-metadata imbalance (a taxed sale is legitimately Dr 118,000 / Cr 100,000) but strict once explicit tax legs are present. Runs before the cash check in the combined retry message, since fixing the legs is what lets the cash walk see anything.

**(3) Tie-out account matcher swallowed sibling accounts (pre-existing).** Containment matching let "Sales" match BOTH "Sales" and "Sales Returns", so each was compared against the SUM of the two and tie-out could never succeed for any submission using a returns ledger — this alone would have kept failing even after fix (1). `checkTrialBalanceTieOut` now resolves exact name matches first and only falls back to the fuzzy split-account rule for rows no other expected account has claimed exactly, preserving the "Credit Sales A/c + Cash Sales A/c both map to Sales" behaviour it was written for.

Verified false alarms (checked, NOT bugs): the diagnostic's 47 "Dr != Cr" transactions are the documented GST-as-metadata design (Dr 112,100 − Cr 95,000 = exactly the 17,100 IGST); diagnostic tie-out failures reflect real learner errors; Inngest's /.netlify and /.redwood 404s are framework auto-discovery.

12 new tests (199 total), tsc/lint/build clean. STILL OPEN: three delivered batches (Garima, Praveen, template595) remain unpostable and need regeneration; template595's invoices still contradict its key.

## Session log — 2026-09-02: CASH/BANK FEASIBILITY — generated batches must be POSTABLE

Garima reported: "I am facing an issue with closing balance cash value in bank statement. According to my previous entry its says i don't have sufficient cash to transfer it to bank." She was right, and it is a generator bug, NOT a scoring or document bug (the answer key and PDF agreed with each other perfectly — the TRANSACTION ITSELF was impossible).

Root cause: batch generation had zero visibility into ledger balances. `company_transaction_log` stores voucher types, ledger names and counts — never amounts — so the model invented plausible-sounding cash figures. Netting her real answer keys: April pack leaves Cash at exactly ₹19,900; her delivered batch opens with a ₹45,000 cash deposit, driving Cash to −₹25,100.

Fix (same grounded-then-verified pattern as the invoice/month/trading-mix fixes):
- `getExpectedCashPosition` (company.ts): nets Cash and Bank across EVERY prior answer key for the learner (opening_balances + entries), i.e. the CORRECT position — deliberately not the learner's own possibly-miskeyed exports, since an exercise must be solvable by someone who posted correctly. Verified against live data: returns 19,900 / 867,186 for Garima.
- Prompt gained an OPENING BALANCES hard-requirement block stating both balances, that cash may never go negative and the bank may never be overdrawn, that deposits are capped by cash on hand at that moment, and to size movements to the position rather than round-sounding figures.
- `checkCashFeasibility` (generate-exercise.ts, pure/exported): walks the batch in sequence order applying cash/bank movements to the opening position and rejects naming the first overdrawing transaction; feeds the same combined retry message as composition/month/doc-pointer. Order-sensitive by design (a deposit before the withdrawal that funds it is correctly rejected).
- 7 new tests incl. Garima's exact numbers, plus a live-data check confirming her delivered batch is caught at transaction 1. 192 total, tsc/lint clean.

NOTE: her CURRENT batch is already delivered and still contains the impossible transaction — needs regeneration or a manual amount adjustment; the fix prevents recurrence only.

## Session log — 2026-09-01 (later 6): EXPIRED PDF LINKS + LIVE ANSWER-KEY/PDF MISMATCH

**(a) Expired document links.** Learners hit Supabase Storage `{"error":"InvalidJWT","message":"\"exp\" claim timestamp check failed"}` on View. Cause: signed URLs are minted when the chat page RENDERS, with a 1-hour TTL — any tab left open longer (or an older batch revisited) served a dead link. Fixed both ways: TTL 1h → 24h in source-documents.ts and exercise-packs.ts, AND sign-on-click — new `freshSignedUrlForDocument` (by ROW ID; the storage path is read server-side, never trusted from the client) / `freshSignedUrlForPackFile` (by path; 'packs' is shared authenticated content), exposed through the `refreshDocumentUrl` server action, called by DocumentCard (now a client component) which opens the tab synchronously BEFORE awaiting so pop-up blockers don't kill it, and falls back to the render-time href if the refresh fails. MessageBubble distinguishes the two card kinds by `id.includes('/')` (pack ids ARE storage paths; document ids are uuids).

**(b) Generated invoices contradict their answer keys.** User caught invoice dates all reading "2024-01-15"; investigation found a worse problem — the vendor-invoice generator is grounded on ONE leg only (unlike the bank statement, which got full per-line grounding on 2026-09-01), so its figures disagree with the key. Audited both live interns: Garima 3/3 invoices wrong (DT-114 70,800→PDF 60,000; MS-331 59,000→PDF 50,000 with IGST 0.00; CS-089 41,300→PDF 35,000), Praveen 1/2 wrong (DT/334 59,000→PDF 69,620). Since doc-backed transaction text carries NO figures (fix #4), the PDF is the learner's only source — posting faithfully would be marked wrong. All 5 PDFs verified internally consistent (lines+tax=total), so answer keys were realigned TO the PDFs; a simulated faithful posting then scores 100% with TB tie-out for both. Applied via SQL handed to the user (direct DB writes from scripts are blocked by the tooling classifier); originals backed up to BACKUP_{garima,praveen}_answerkey.json.

**INVOICE-GENERATOR ROOT CAUSE — FIXED (same day):** vendor invoices are now grounded and validated exactly like the bank statement:

- `VendorInvoiceInput` = the transaction's COMPLETE leg set + its description; `planSourceDocuments` carries `{legs, transactionDescription}` for invoices (was one entry + party names).
- `deriveInvoiceFigures` (source-document.ts prompts, pure/exported): base = Dr non-tax legs, tax = GST-named legs by head, total = Cr party leg; single-leg keys split the inclusive party total by gst_rate (default 18%). `extractTransactionDate` pulls the date from the description (same token shapes as checkBatchMonth); `formatInvoiceDate` renders DD-MMM-YYYY.
- `buildVendorInvoicePrompt` states the figures as HARD requirements (exact vendorName, exact invoiceDate, exact bill number, lineItems summing to base, exact per-head tax, exact total). Old single-entry buildSourceDocumentPrompt/RetryPrompt + generateSourceDocument DELETED (only caller migrated).
- `generateVendorInvoiceDocument` + `checkVendorInvoiceContent` (generate-source-document.ts, exported): deterministic post-validation — lineItems sum == base, totalAmount == party total, per-head tax matches (null-vs-0 tolerated on unused heads), invoiceDate == the transaction's own date (DD-MMM/numeric/ISO all accepted), vendorName matches the party account — violations feed the bounded retry, so a document-vs-answer-key contradiction can no longer be RENDERED.
- 13 new tests including the exact live failures (60,000-total DT-114, IGST-0.00 MS-331, "2024-01-15" dates, invented "Shree Traders") — 184 total. tsc/lint clean.

## Session log — 2026-09-01 (later 5): LANGFUSE COST + EVAL WIRING

Langfuse was tracing calls (input/output/errors/latency, userId=learner) but showed NO cost/tokens: the model name never reached trace.generation() and OpenRouter's usage block was discarded by client.ts. Wired end to end:

- client.ts: getStructuredCompletion now returns `{ output, model, usage }` (StructuredCompletionResult); requests OpenRouter usage accounting (`usage: { include: true }`) — response carries prompt/completion/total tokens AND the actual charged `cost` in USD (live-verified: haiku call returned cost 0.000033). `model` echoes the resolved id.
- tracing.ts: generation() gets `model` (requested, overwritten on end with the served id); generation.end() gets `usage {input/output/total, unit TOKENS}` + `costDetails { total: costUsd }` — Langfuse dashboards (cost by model/call-type/user, tokens, latency) light up with ACTUAL OpenRouter-charged cost, no Langfuse price-table entries needed.
- NEW `recordSubmissionScore` (tracing.ts), called fire-and-forget from run-scoring's persist step: each scored submission creates a 'submission-scored' trace (userId=learner, metadata submissionId/overallResult) with Langfuse SCORES weighted_score (0-1), passed (1/0), tb_tie_out (1/0) — model/prompt changes can now be correlated with learner outcomes in Langfuse → Scores.

Gates: tsc/lint clean, 171 tests, live OpenRouter usage smoke test.

## Session log — 2026-09-01 (later 4): SCHEMA-IN-PROMPT — OpenRouter does NOT enforce json_schema for Claude

Adaptive generation failed 3/3 attempts (~7 min wasted) with every transactions[].sequence/description and answer_key entries[].sequence/voucher_type undefined plus an invalid difficulty_level — systematic, not random. Root cause: OpenRouter only natively enforces response_format json_schema for some providers; with Claude the model NEVER SAW the schema (also the true root of the earlier fence issue), so it invented its own field names. Fix in client.ts: the JSON schema is now ALSO appended in-band as a final system message ("SINGLE JSON object... these exact key names... every required field: <stringified schema>"); response_format stays on for providers that do enforce it. Live smoke test against anthropic/claude-sonnet-5 via OpenRouter confirms: no fences, exact keys, valid enum, first attempt. Gates: tsc/lint clean, 171 tests. Failed production runs can be replayed with Inngest's Rerun (completed steps memoize; only generation re-executes).

## Session log — 2026-09-01 (later 3): PRODUCTION SLOWNESS — FENCED-JSON CRASH FIX + PARALLEL DOCS + FAST DOC MODEL + POLL WINDOW

Production Inngest trace (run-scoring 9m20s) exposed the real bug behind the "slow" next-batch step: **Claude Sonnet 5 via OpenRouter returns JSON wrapped in a markdown code fence** ("```json ... ```") despite response_format json_schema; client.ts's raw JSON.parse threw `Unexpected token '\`'`, and Inngest retried the WHOLE generation step (attempts of 2m21s + 2m49s + 6m6s — batch + all PDFs regenerated each time). gpt-4o-mini never fenced, so this only surfaced after the model upgrade. Fixes:

1. **client.ts `extractJsonPayload`**: strips a single wrapping ```/```json fence before JSON.parse; anything else still fails loudly. THE critical fix — model-portability guardrail.
2. **Per-call model override**: StructuredCompletionParams gains optional `model` (threaded through tracing.ts); both source-document call sites pass `process.env.OPENROUTER_DOCUMENT_MODEL` (set to anthropic/claude-haiku-4.5 in .env.local) — documents are simple structured content, Haiku is ~3-5x faster; falls back to OPENROUTER_MODEL when unset.
3. **prepareSourceDocuments parallelized**: all invoice docs + the combined statement generate CONCURRENTLY via Promise.all (previously strictly sequential — 5 doc calls dominated the tail). Deterministic result order kept.
4. **PendingSubmission NEXT_EXERCISE_POLL_LIMIT 60→144** (5→12 min): generation exceeding the old 5-min poll budget meant the next exercise silently never auto-arrived.

NOTE for VPS deploy: add OPENROUTER_DOCUMENT_MODEL=anthropic/claude-haiku-4.5 to the VPS .env alongside OPENROUTER_MODEL. Gates: tsc/lint clean, 171 tests.

## Session log — 2026-09-01 (later 2): FEEDBACK GUARDRAILS + MODEL UPGRADE TO CLAUDE SONNET 5

After verifying the intern's second attempt scored correctly (84, engine-reproduced exactly), three remaining feedback-prose polish issues were fixed with deterministic guardrails (user's directive: enterprise accuracy via verification, not model trust) plus a model upgrade:

1. **Identifier-echo guard** (`checkFeedbackIdentifiers`, generate-coaching.ts): every identifier-like token (uppercase hyphenated refs: DT-115, INV-M-101, CA26-101) in the model's went_well/needs_work must literally appear in the scoring signal — a live bullet printed "DW-115" for DT-115. Violations retry alongside checkOpeningLineFacts; after MAX_ATTEMPTS the bullet lists are replaced with code-composed ones (needs_work from incorrectConceptDescriptions verbatim, went_well emptied) rather than shipping invented references.
2. **Praise/flag exclusivity** (buildCoachingSignal): transactions with ANY flagged field are dropped from the praise side — "DT-115 handled well" + "revisit DT-115" in one feedback was factually consistent (different fields) but read as self-contradiction.
3. **Label-collision disambiguation** (buildSequenceLabels): duplicate labels (two "Bank Charges payment" sequences) get the transaction amount appended ("of Rs. 350" / "of Rs. 850") — an answer-key fact also present in the learner's own source documents, so no key leakage.
4. **Model**: OPENROUTER_MODEL switched from openai/gpt-4o-mini to **anthropic/claude-sonnet-5** in .env.local. NOTE: the VPS .env must be updated by hand (env files are gitignored) and the first live call should be watched to confirm OpenRouter's structured-output translation for this model.

5 new guardrail tests (171 total). tsc/lint clean.

## Session log — 2026-09-01 (later): DOC-DELIVERY RACE + STATEMENT ACCOUNT-HOLDER FIXES (from the user's local test of the 5 batch fixes)

User's localhost test of a fresh batch surfaced two bugs: (a) the batch arrived in chat with pointer text but NO PDF cards — DB showed the docs WERE generated (4 invoices + exactly 1 combined statement, confirming fix #1 live) but the exercise row landed at 07:00:10 and the last doc row at 07:00:42, while PendingSubmission's 5s next-exercise poll delivers the exercise as soon as its row exists → delivered docless; a refresh (timeline rebuild) showed all cards. (b) the combined statement was titled "Bank Statement — ABC Trading Co." — the batch prompt said "the account holder is the business itself" without naming it.

Fixes in generate-exercise.ts / source-document.ts / generate-source-document.ts: `generateAndAttachSourceDocuments` split into `prepareSourceDocuments` (ALL slow work — LLM + render + upload — now runs BEFORE insertExercise, against a pre-generated batch-id storage folder since the bucket's RLS only scopes the learnerId segment; a doc failure now aborts before any exercise row exists, so a docless batch can never be delivered) + `attachSourceDocuments` (FK-dependent doc-row inserts right after insertExercise — milliseconds). Bank-statement generation now takes companyName (threaded from getCompanyName, 'Blossom Retail Pvt Ltd' fallback; legacy generated-diagnostic path pins the constant) and the prompt pins account holder EXACTLY to it. NOTE: an earlier working-tree copy of these same race-fix edits was silently reverted on disk (OneDrive/editor sync) before gates ran — re-applied and verified; watch for that failure mode in this OneDrive-hosted repo. 166 tests, tsc/lint clean.

## Session log — 2026-09-01: ONE COMBINED BANK STATEMENT PER BATCH (fix #1 of the user's 5-point generated-batch review)

Live Module-2 batch delivered THREE separate "Bank Statement — HDFC Bank.pdf" cards plus nonsense "Invoice — HDFC Bank.pdf"/"Invoice — TDS Payable.pdf" cards. Root cause: generateAndAttachSourceDocuments generated one PDF per flagged answer-key ENTRY (per leg, not even per transaction), and trusted the LLM's source_document_type. Fixed:

- `planSourceDocuments` (generate-exercise.ts, new pure/exported): dedupes flagged entries by sequence (multi-leg transactions previously made duplicate PDFs), deterministically forces Contra/Receipt/Payment voucher types to bank_statement (a transfer/settlement never arrives as an invoice), and splits into per-bill vendor invoices vs. bankLines destined for ONE combined statement.
- `generateBankStatementDocument` (generate-source-document.ts, new): single LLM call for the whole batch's statement via `buildBankStatementBatchPrompt` (source-document.ts, new) — one line per bank transaction, chronological, running balance, real-bank narrations, dates grounded in each transaction's own exercise description (answer-key entries carry no date field); retried when the line count doesn't equal the input transaction count. Schema unchanged (BankStatementContentSchema was always multi-line); renderer unchanged (content-driven).
- Adaptive prompt hardened with the same only-bank_statement-for-Contra/Receipt/Payment rule (defense in depth; the code guard decides regardless).
- 5 new planSourceDocuments tests (146 total). tsc/lint clean.

**#2 TRADING-MIX FLOOR (done, same day):** checkBatchComposition now also counts transactions by answer-key voucher type and requires ≥2 Sales and ≥2 Purchase per non-escalation batch (MIN_SALES_TRANSACTIONS/MIN_PURCHASE_TRANSACTIONS) — the observed pure-bank Module-2 batch is rejected even when concept counts pass. Applies in the no-strengths-yet case too (trading month needs trading activity regardless of the concept plan); escalation stays exempt (deliberately narrow drills). All violations (trading mix + concept split) are now COLLECTED into one retry message instead of surfacing one rule per attempt, so a retry fixes everything in a single pass. Prompt's BATCH COMPOSITION block gained the same rule ("bank-side targets are practiced alongside the month's trading activity, never instead of it") plus a count-by-voucher-type self-check. GST-per-party-state realism stays prompt-enforced only (deliberate: exempt/unregistered cases exist, so gst_head non-null is not hard-required). 3 new tests + 2 updated (149 total). tsc/lint clean.

**#3 MONTH-PER-BATCH TIMELINE (done, same day):** the batch month is now computed in code, never guessed by the LLM (the company log stores no dates, so the old "month following the latest transaction" instruction had nothing to anchor on — observed live as one batch mixing May and June). `exerciseMonthForModule(ordinal)` (generate-exercise.ts, pure/exported): ordinal 1 = the diagnostic pack's April 2026, +1 calendar month per exercise, year rollover handled. run-scoring passes `priorExerciseCount + 1` — DELIBERATELY anchored on exercise count, not getModuleNumber (mastered-concepts+1), so the month advances exactly once per batch regardless of mastery speed (module-number anchoring would repeat April for a slow learner and skip months for a fast one; flagged to user in planning, user approved via "Do the changes" on a plan noting this). Prompt: hard "EVERY transaction inside <Month Year>" rule with explicit per-line dates; the educational-mode "across two consecutive months" line (the direct cause of the May+June mix) removed — educational spreads within the single month's 1st/2nd/last days. `checkBatchMonth` (pure/exported) scans transaction descriptions for dated tokens ("01-Jun-2026" and numeric DD-MM-YYYY) and feeds any wrong-month/year date into the same combined retry message as composition; positive mismatches only (no-date phrasings and bare "the March invoice" mentions don't fail). 8 new tests (157 total). tsc/lint clean.

**#4 DOCUMENT-BACKED TRANSACTIONS ARE POINTERS (done, same day):** when a transaction ships as a PDF, its numbered text line no longer restates the figures (observed live: every doc-backed line spelled out amount + GST, making the PDFs decorative). Prompt gained a DOCUMENT-BACKED TRANSACTION TEXT hard rule: pointer lines carry date + party + what happened + which document to read, NEVER the amount/GST figure or rate; undocumented transactions keep full explicit details. `checkDocumentBackedDescriptions` (generate-exercise.ts, pure/exported) enforces it: scans doc-flagged transactions' descriptions for rupee amounts (₹/Rs patterns) or percentages (GST rates) and feeds violations into the same combined retry message as composition/month. Dates, "HDFC Bank — 1234", and invoice ids like #DT2026 deliberately don't match the leak patterns. 5 new tests (162 total). tsc/lint clean.

**#5 COMPANY PINNED BY NAME (done, same day — closes the 5-point review):** the generation prompt now opens its company block with "THE COMPANY IS: <name> (home state Karnataka, GST state code 29)" in BOTH branches (including the empty-registry first-exercise branch, which previously said "introduce ledger names freely" with no company at all — the exact gap behind the 2026-08-24 fictional-Maharashtra-company incident). Name comes from new `getCompanyName` (company.ts): reads the OLDEST company_transaction_log row's `company` field (written at pack assignment) — the recent-slice query returns the NEWEST 10 rows, so the name silently fell out of the prompt once a learner accumulated 10+ log entries. Fallback 'Blossom Retail Pvt Ltd' for pre-field rows. New adaptive-exercise.test.ts pins company/month prompt content (4 tests, 166 total). tsc/lint clean. ALL FIVE POINTS OF THE 2026-09-01 REVIEW ARE NOW IMPLEMENTED — needs VPS redeploy to reach learners.

## Session log — 2026-08-31 (later 2): SELF-LOCATING FEEDBACK LABELS

The real intern reported verbatim: "INV-M-101 doesn't exist in my Tally but it's asking me to fix it." The flag was genuine (her ₹75,000 Karnataka Emporium receipt was credited to an invented "KAREM MARCH" ledger with no Against Ref) but the label was unfindable: bill references come from the SOURCE PACK, and a learner who skipped the bill-by-bill step has no "INV-M-101" anywhere in their books. `buildSequenceLabels` (generate-coaching.ts) now pairs every bill-ref label with the transaction's party/voucher-type — "INV-M-101 (the Karnataka Emporium receipt)" — so the label locates the entry in the learner's own books even when the ref is absent from them. Verified on the live intern XMLs; tsc/lint/141 tests clean.

## Session log — 2026-08-31 (later): AUTH FLOW SIMPLIFIED — no email confirmation, explicit signup→login cycle, chat Log out

User disabled "Confirm email" in the Supabase dashboard (their step) and asked for a plain intern-friendly flow: sign up → land on login → log in; obvious Log out everywhere; logout fully ends the session. Code changes:

- `app/(auth)/login/actions.ts` signUp: with confirmation off, Supabase returns a live session on signup — the action now signs that auto-session out immediately and returns `accountCreated: true` instead of redirecting to /dashboard, so the learner always logs in explicitly. The confirm-email path is kept as a defensive fallback if the dashboard toggle is ever re-enabled.
- `auth-form-state.ts`: added `accountCreated` to the form state.
- `AuthForm.tsx`: on `accountCreated`, flips to log-in mode (guarded render-time state adjustment) and shows "Account created. Log in below with the email and password you just chose." above the login form.
- `ChatShell.tsx`: added a slim persistent header (✦ AIA Academy + Log out button calling the existing `logOut` server action) — the chat screen previously had NO logout; the only one lived on the dashboard. Logout semantics unchanged and confirmed: `logOut` signs out and redirects to /login, and proxy.ts bounces any signed-out visit to /dashboard//chat//progress//onboarding back to /login.

Gates: tsc clean, eslint clean, 141/141 tests. NOTE: fixes are local — the VPS still runs the old build until redeployed.

## Session log — 2026-08-31: SCORING-FEEDBACK ACCURACY FIXES — audited against the first real intern submission

The user ran a real intern (Garima) through the Blossom Variant A pack in production and asked whether the 82%/"needs work" feedback was accurate. A full manual audit of her DayBook/TrialBal XMLs (note: Tally exports are UTF-16LE and carry illegal `&#4;` XML entities — 1,006 of them — which must be stripped before any standards parser accepts the file) against the answer key found the grade broadly fair but three citation-accuracy defects in the deterministic signal (the coaching LLM was faithfully phrasing a faulty signal). All three fixed:

1. **Unassessed fields no longer flagged** (`generate-coaching.ts` buildCoachingSignal, now exported): when a leg's account never matched, the dr_cr/amount diffs are emitted incorrect-with-null-error-code; they were feeding the flagged areas, telling the intern to "reconsider the debit and credit direction" on INV-010, whose direction was textbook-correct. The signal now only includes diffs carrying a real error code.
2. **Missing transactions named as missing** (`generate-coaching.ts`): VOUCHER_MISSING (hosted on the account field) read as a "ledger account classification" — or worse, GST — problem. Missing entries now get their own flagged-area line: "entries that appear not to have been recorded in the Day Book at all (AI-201, …)".
3. **Voucher-stealing matcher fixed** (`score-submission.ts` matchVouchersToTransactions): generic ledger legs (Sales/Purchase/Cash/bank/GST heads) scored +4 same as distinctive party legs, so a transaction the intern never posted greedily grabbed any unused voucher sharing a generic "Purchase" leg and every field of that innocent voucher was flagged against the wrong key ("re-check GST on AI-201"). Similarity is now distinctiveness-weighted (distinctive account 4, generic 1, amount 1, type 1) with MIN_MATCH_SCORE 4; the blind positional fallback only applies when vouchers ≤ key transactions (short drills), so pack exports report VOUCHER_MISSING instead.
4. **Half-posted intra-state GST pair now caught** (`score-submission.ts` diffGst): first-match head inference + CGST~SGST equivalence let HR-118's real error (CGST posted twice, SGST absent) pass silently. diffGst now collects ALL posted heads; intra-state requires the complete CGST+SGST pair (no IGST), inter-state requires IGST alone; a split-missed half maps to GST_MISSING (Appendix A E05), wrong regime stays GST_HEAD_WRONG. Side check now fails on ANY contradicting stated side (catches the cash sales credited to "Input CGST/SGST c/f").

Verified: 141/141 tests (5 new regression tests pinning each defect), tsc clean, eslint clean, and a live re-score of the actual intern XMLs through the fixed pipeline — INV-010 now shows as a direction strength, AI-201/Bank Charges/Packing Materials/Output CGST journal correctly reported as never recorded, HR-118 now flagged under GST. Every remaining flagged item hand-verified genuine against the vouchers.

## Session log — 2026-08-28 (later): landing dashboard preview re-contented to OUR product

User (with a screenshot of the fintech mock) asked for the hero's coded dashboard preview to show this tool instead. `DashboardPreview.tsx` content swapped, structure/classes untouched: top-bar CTA "Start Exercise" (was Move Money); sidebar = Home / Tutor Chat (badge 2) / Exercises / Submissions / Progress / Modules / Certificate + a Concepts section (GST, TDS, Bank Entries, Narrations, Settings); action pills = Start Exercise (accent), Upload Day Book, Upload Trial Balance, Ask Tutor, Get a Hint, View Progress; balance card became "Mastery Score" 92.4% with "+8 passed" / "2 to retry" and the same Bézier chart; accounts card became "Concept Areas" (GST Postings 96%, TDS Sections 88%, Bank Reconciliation 74%); transactions became "Recent Submissions" (Batch 7 Bank Statement "Scoring" amber, Batches 6/5/4 Passed, top score green). tsc + lint clean; ui-context.md preview bullet updated.

## Session log — 2026-08-28: NEXORA-STYLE RETHEME — landing, auth, dashboard rebuilt; Wandor theme replaced (frontend only)

User supplied a second, replacing design spec (a "Nexora" SaaS hero): Instrument Serif display headings with italic emphasis words + Inter body, white canvas, dark charcoal ink (`210 14% 17%`), indigo accent (`239 84% 67%` ≈ #6366F1), shadcn-style `hsl(var(--token))` tokens, framer-motion fade-up entrances, and a fully CODED dashboard preview clipped at 100vh. Applied across landing, login, onboarding, and the app dashboard with this project's AIA Academy branding/content. **No backend code touched** — actions, auth logic, redirects, queries unchanged.

- **Fonts**: Google Fonts link swapped to Instrument Serif (400 + italic) + Inter (400/500/600); `font-sans`/`font-body` → Inter, `font-display` → Instrument Serif. Geist/Special Elite retired.
- **Tokens**: `globals.css` now defines the spec's shadcn HSL triplets (`--background/--foreground/--primary/--secondary/--muted/--accent/--border/--ring/--radius/--shadow-dashboard`) mapped in Tailwind as `hsl(var(--token))` (accent/primary/secondary/muted have `foreground` sub-keys; `shadow-dashboard` boxShadow added). Legacy app tokens (`--bg-canvas`, `--text-primary`, `--bg-user-bubble`…) kept and repointed (indigo-50 user bubble, indigo ai-thinking, #e5e5e5 borders) so chat/progress restyle with zero markup churn. All `wandor-*` keys and usages removed (repo-wide grep clean).
- **New landing** (`app/page.tsx`): `h-screen flex flex-col overflow-hidden` = Navbar + Hero at exactly 100vh. `Navbar.tsx` (✦ AIA Academy wordmark, muted nav links, Get started pill → /login). `Hero.tsx` (client): fullscreen CloudFront video, framer-motion fade-up sequence (badge → headline "The Future of *Smarter* Bookkeeping" → sub → CTAs "Start training" + circular Play → preview), `DashboardPreview.tsx` — the spec's coded fintech mock (top bar/sidebar/balance card with hand-crafted cubic-Bézier SVG area chart in accent/accounts/transactions table), branded "AIA Academy"/"Welcome, Elina"/avatar "ES", "Mercury Balance" renamed "Primary Balance" (real-brand name avoided), spec's "Trake rutes" typo written as "Track routes". Semantic tokens only; status pills use the existing status-success/warning tokens.
- **Auth**: `(auth)/layout.tsx` keeps the shared video backdrop + wordmark; login/onboarding cards are now `rounded-2xl backdrop-blur-xl` frosted panels (`rgba(255,255,255,0.7)` + `--shadow-dashboard`) with Instrument Serif headings; inputs `rounded-lg border-border` with indigo focus ring; submits use the new shadcn-style `Button`.
- **`app/components/ui/button.tsx` (new)**: shadcn-style Button + `buttonVariants()` helper (default/ghost/outline, default/icon sizes), no cva/radix deps. `framer-motion` installed; `tailwindcss-animate` deliberately skipped (v3 plugin, nothing uses it).
- **Hydration fix**: `suppressHydrationWarning` restored on `<html>` and added on `<body>` (it was dropped in the Wandor pass when the theme script was removed; extension-injected attributes then surfaced mismatch warnings). No other mismatch sources found (no Date/random/window-dependent render paths in the changed components).
- **App dashboard**: same content (Modules "Coming soon" / Task → /chat / Log out via existing action), new theme: wordmark header + outline Log out pill, Instrument Serif "Welcome *back*", `rounded-2xl` cards, accent Task eyebrow, primary arrow circle, hover lift + `shadow-dashboard`.
- `context/ui-context.md` rewritten for the new system. Gates: tsc clean, lint clean, 136/136 tests, build clean.

## Session log — 2026-08-27 (later 7): PRODUCTION DEPLOYMENT — LIVE, confirmed working end to end at https://ai-tutor.187-127-173-25.sslip.io

Continuation of the deployment session below: the user actually ran the deploy on the VPS, hit two real infrastructure mismatches versus the first-pass assumptions, both diagnosed live via `docker inspect`/`docker logs` on the VPS and fixed:

1. **`traefik-proxy` network didn't exist** — `docker network ls` on the VPS showed no shared external network at all; every app (`aia-flagged-automation`, `gm-opportunity`, etc.) sits on its own isolated `_default` bridge network. `docker inspect traefik-traefik-1` revealed why: Traefik runs with **`network_mode: host`**, not on any bridge network, and discovers containers purely via Docker labels (socket mounted read-only, `exposedbydefault=false`). It reaches apps through a **fixed `loadbalancer.server.url=http://127.0.0.1:<port>`**, confirmed from `aia-flagged-automation`'s own labels (`http://127.0.0.1:8000`) — not by joining a shared network and using `.server.port`.
2. **Rewired `docker-compose.yml` to match**: dropped the nonexistent `networks: traefik-proxy` block entirely; app now publishes `127.0.0.1:${APP_PORT:-3005}:3000` (loopback-only, matches `karbon-mis`'s identical 3000-container-port pattern) and the label points `loadbalancer.server.url` at that same loopback port. Committed as `64fd9fd`.
3. **New cert needed one Traefik restart** — the domain landed in `acme.json` (ACME request succeeded) immediately on first `docker compose up -d --build`, but the live TLS handshake kept serving a self-signed fallback until `docker restart traefik-traefik-1`. One-time per new domain; safe (other apps' cached certs aren't re-requested). Documented as an expected step, not a bug to route around.
4. Confirmed live: `docker compose ps` → healthy; `curl -Iv https://ai-tutor.187-127-173-25.sslip.io` → `HTTP/2 200`, valid Let's Encrypt cert, no warnings.
5. **`.env` on the VPS already correct** — user had already removed `INNGEST_DEV` and set real prod `INNGEST_EVENT_KEY`/`INNGEST_SIGNING_KEY` before this session's checklist even flagged it (verified via `grep INNGEST .env`).
6. Docs corrected to match confirmed reality (not the earlier generic-Hostinger-docs assumption): `DEPLOYMENT.md` rewritten with the actual Traefik host-network + fixed-URL wiring, the live URL, the one-time restart step, known-taken ports on this VPS (8000-8003) so a future app picks a free one; `architecture.md` §6 updated; `.github/workflows/deploy.yml` path fixed from the wrong `/opt/apps/ai-tutor` guess to the real `/opt/Training-Module`.

**Still outstanding (user, not yet confirmed this session):** Inngest Cloud app sync at `https://ai-tutor.187-127-173-25.sslip.io/api/inngest`; Supabase Auth Site/Redirect URL update to the same domain; one full live learner submission to confirm scoring actually completes end to end in production (the one thing curl can't verify — needs the Inngest job to fire for real).

## Session log — 2026-08-27 (later 6): PRODUCTION DEPLOYMENT SETUP — Docker + compose + runbook for the Hostinger VPS (code-complete; NOT yet deployed)

User has a Hostinger VPS (`srv1701205.hstgr.cloud` / 187.127.173.25, KVM 8, Docker Manager with existing containers incl. Traefik and Uptime Kuma) and asked for a proper production deployment. Built the full containerization + runbook; **no server-side steps executed yet — deployment itself is the user's next action.**

- `next.config.ts`: `output: 'standalone'` (self-contained `server.js`, no node_modules in the runtime image). Build verified clean with the change.
- `Dockerfile`: 3-stage (npm ci → build → node:22-alpine runner), non-root `nextjs` user, `NEXT_PUBLIC_*` as build ARGs (inlined client-side at build time), `HOSTNAME=0.0.0.0`.
- `.dockerignore`: excludes `.env*` (secrets never enter build context/layers), node_modules, context/, xmls/, seed/, scripts/, supabase/.
- `docker-compose.yml`: **Traefik-routed by default** (user follow-up — wanted the VPS's existing Traefik on from the start, not opt-in). Joins the external `traefik-proxy` network (Hostinger Docker Manager's default template name), labels route `Host(\`${APP_DOMAIN}\`)` on `websecure`/`letsencrypt`. **Corrected mid-session**: first pass wrongly hardcoded `srv1701205.hstgr.cloud` as the fallback domain — user caught it ("the treafic domain is domain name.sllipts.com or somehjing"); Hostinger's actual template uses a per-VPS `TRAEFIK_HOST` (confirmed via WebFetch of Hostinger's own Traefik-in-Docker-Manager doc, plus user confirmation it's a sslip.io address). `APP_DOMAIN` now has NO default and is documented as required, sourced from the Traefik container's own env var or an existing app's "Open app" URL suffix. No `ports:` mapping at all now (was briefly `${APP_PORT:-3005}:3000` as the primary path, causing the user's "3000 already in use" concern) — Traefik reaches the container over the internal Docker network only (`expose: 3000`, never bound to the host), so it cannot collide with any other container's host port 3000; the host-port form survives only as a commented fallback block for if this VPS's Traefik network isn't actually named `traefik-proxy`(must verify with`docker network ls` before first deploy — flagged prominently in DEPLOYMENT.md).
- `app/api/health/route.ts` (new): liveness-only probe (deliberately touches no dependency) for the compose healthcheck + Uptime Kuma.
- `.github/workflows/deploy.yml`: manual-dispatch deploy (SSH → git pull → compose up --build → image prune); needs `VPS_HOST`/`VPS_USER`/`VPS_SSH_KEY` repo secrets. Push-trigger auto-deploy documented as an opt-in one-liner.
- `DEPLOYMENT.md` (new, the runbook): one-time prereqs (production `.env` — **`INNGEST_DEV` must be REMOVED** and real Inngest keys set; Langfuse keys reachable-or-deleted; Supabase Auth Site/Redirect URLs → `https://srv1701205.hstgr.cloud`; `seed-pack.mjs` run from local against prod; **Inngest Cloud app sync at `https://srv1701205.hstgr.cloud/api/inngest` after first boot** — scoring queues forever without it), first-deploy SSH steps (verify with `curl -I` against the domain, not a bare port), update procedure, ops table, troubleshooting (added: 502/404-but-container-healthy → Traefik network mismatch).
- `architecture.md` Deployment section updated with the Traefik-by-default routing detail.
- Gates: build clean (standalone `server.js` emitted, `/api/health` in the route table). Pending (user, on the VPS): clone → `.env` → confirm `traefik-proxy` network name → `docker compose up -d --build` → Inngest sync → live smoke test per DEPLOYMENT.md.

## Session log — 2026-08-27 (later 5): WANDOR REDESIGN — landing, auth, dashboard restyled; dark mode removed (frontend only)

User supplied a full "Wandor" landing-page design spec (Geist body font, Special Elite typewriter wordmark, white minimal palette, terracotta `#905831` accent, pill buttons, frosted-glass card over a looping background video) and asked for it to be applied across the product with this project's content: landing, login/logout, onboarding, dashboard, and the inner surfaces inheriting the same fonts/colors. White only, no dark theme. **No backend code touched** — server actions, auth logic, redirects, queries all unchanged.

- **Fonts**: Google Fonts `<link>` (preconnect + Special Elite 400 / Geist 400-700) in `app/layout.tsx`; Tailwind `font-sans` → Geist, new `font-display` → Special Elite. `lucide-react` installed (Upload, ArrowUpRight icons).
- **Dark mode removed**: theme init script deleted from layout, `.dark` block deleted from `globals.css`, `darkMode: "class"` removed from Tailwind config, `ThemeToggle.tsx` + `TokenSwatches.tsx` deleted (only the old landing used them; no `dark:` variants existed anywhere).
- **Tokens repointed, not renamed** (`globals.css`), so every existing component restyles itself with zero markup churn: ink `#1a1a1a`, accent black `#0a0a0a` (blue `#314DD0` retired), user bubble/accent-subtle warm sand `#f4efe9`, ai-thinking terracotta pulse, warm borders. New Tailwind `wandor.{dark,text,muted,prompt}` colors.
- **New landing** (`app/page.tsx` → `app/components/Hero.tsx` + shared `VideoBackdrop.tsx`): spec-faithful hero — nav (wordmark "AIA Academy", center links, Login + Start Training pills → /login), clamp headline "Ready to run real books?", project subtitle, liquid-glass prompt card with terracotta sample learner prompt, `.xml` upload button, black CTA.
- **Auth**: new `app/(auth)/layout.tsx` renders the same video backdrop + wordmark for login AND onboarding; both pages became `bg-white/[0.55]` glass cards; AuthForm/OnboardingForm inputs restyled (`rounded-2xl` glass inputs, black pill submits, terracotta links/notes). All form behavior identical.
- **Dashboard**: landing-style nav (wordmark + Log out outline pill), "Welcome back." headline, two `rounded-[32px]` cards — Modules placeholder (muted, "Coming soon") and Task (black-bordered, terracotta eyebrow, arrow-circle CTA, hover lift → /chat).
- Chat + progress pages untouched by design (they inherit fonts/tokens); `context/ui-context.md` rewritten for the new system.
- Gates: tsc clean, lint clean, tests pass, build clean (see below). Live check pending: video loads from the external Figma URL (page still reads fine white-on-white if it ever 404s).

## Session log — 2026-08-27 (later 4): pack answer-key leak via help button FIXED (136/136)

Live find: on the Blossom pack, each help click past step 3 generated a FRESH step 3 that solved a different random transaction (the button carries no which-transaction signal against a 99-transaction key) — clicking repeatedly leaked the authored answer key one entry per click. Two fixes:

1. **Step-3 reuse**: `getLatestDeepHintForExercise` (rung >= 3, covers legacy 4-5 rows); requestHint returns the STORED step-3 content on every later click, no LLM call, no new row.
2. **Pack mode** (`exercise.packFiles.length > 0`): the answer key is WITHHELD from the hint prompt entirely — the model only receives a derived summary (transaction_count, voucher_types histogram, concept_areas), so it cannot quote figures it never saw (3 tests assert no party/amount/reference reaches the prompt). Steps re-aimed: step 2 names the set's care-areas and asks which entry is stuck; step 3 teaches the METHOD with an invented example and invites the learner to name the blocking entry in chat (QA then points at the rule, per its existing boundary). Generated drills keep the full-answer step 3 unchanged.

## Session log — 2026-08-27 (later 3): same-file-twice upload crash fixed, 3 layers (133/133, build clean)

Live find: user attached the 5.6 MB Day Book TWICE (11.2 MB total) → Next's PROXY body cap (10 MB default, separate from serverActions.bodySizeLimit) truncated the stream → "Unexpected end of form" 500 + React duplicate-key warning. Fixes:

1. `next.config.ts`: `experimental.proxyClientMaxBodySize: '25mb'` (the current name; `middlewareClientMaxBodySize` is deprecated in this Next version) — kept in lockstep with serverActions.bodySizeLimit.
2. ChatShell client guards BEFORE upload: exact-duplicate pair (same name + size → "you've attached the same file twice" nudge) and total size > 20 MB ("not a backup or a zip" nudge).
3. MessageBubble attachment chips keyed by name+index (duplicate names no longer collide).
   Langfuse network-timeout noise in the logs is the known unreachable-host issue (fire-and-forget already; harmless). For production: point LANGFUSE_HOST at a reachable instance or remove the keys and the noise disappears.

## Session log — 2026-08-27 (later 2): submit-flow error messages humanized (133/133)

User request: error replies must read like GPT/Claude, conversational and helpful. All learner-facing error strings in `app/(chat)/chat/actions.ts` rewritten in the tutor voice (first person, says what to do next, no em dashes). Key one: uploading XMLs on a review exercise now says "No files needed for this one! It's a written review: look through the books... type what you found" instead of the cold "This exercise does not require file uploads." (context-aware: review vs other text-only kinds). Also softened: two-files-needed, non-XML file, upload failures (own the failure: "nothing you did wrong"), no-active-exercise, question-too-long, typed-answer-on-file-exercise.

## Session log — 2026-08-27 (later): live test round 1 — Phases 1/3 verified, Phase 2 composition ENFORCED after live miss (133/133)

User's live run: Phase 1 feedback rendered correctly (98 percent score-first opener, headed sections, real identifiers INV-012/SA-101, no em dashes) and the batch intro named strengths pilot-style. BUT the generated Batch 2 was 10 contra-only cash-bank transfers — the model ignored the 50/50 instruction ("roughly half" was too soft) and produced exactly what the prompt's own words call "not a batch". Fix, two layers:

1. Prompt hardened: "THIS IS A HARD REQUIREMENT", "AT LEAST 4" per side replaces "roughly half", plus a self-count instruction over concept_tags before responding.
2. **Deterministic enforcement**: `checkBatchComposition` (exported from generate-exercise.ts, 6 tests incl. the exact live failure) counts strength/weakness transactions from the answer key's own concept_tags; a violating batch re-enters the existing MAX_ATTEMPTS retry loop with the violation text as the error. Escalation and no-strengths-yet learners are exempt (legitimately one-sided); 10-12 count enforced for all non-escalation batches. If all 3 attempts stay unbalanced, the last schema-valid batch ships anyway (learner always gets SOMETHING, mirroring the review fallback).
   Next live check: resubmit and confirm the next batch carries at least 4 step-up + 4 reinforcement transactions.

## Session log — 2026-08-27: PHASE 4 IMPLEMENTED (spec 16, code-complete; 127/127, build clean; NOT live-verified) — ALL 4 PHASES NOW CODE-COMPLETE

Spec `16-phase-4-ui-and-content.md` implemented. **Variant B confirmed untouched** (user removal decision honored: no Variant B seeding/selection code added; pack selection stays on the any-pack fallback).

- **Light default theme**: `app/layout.tsx` init script now applies dark ONLY on an explicitly stored "dark" choice — OS dark preference no longer flips it, existing togglers keep their choice, still FOUC-safe (runs before paint). ThemeToggle unchanged.
- **Widescreen**: chat messages and composer live in a centered `max-w-[1150px]` column (previously spanned the full window); progress page widened to match with a 2-column module grid at `lg:`.
- **Concept-area graduation view**: `ConceptStatusBadge` gained `lastAttemptResult`; green "Mastered" for consistently-correct areas, amber **"Keep iterating"** (replacing the "Escalation" label) when escalation is active OR the latest attempt failed, neutral "Developing" otherwise. Page subtitle states the framing. No new data model.
- **Document format variety**: 5 new invoice templates alongside the original (A original, B boxed-classic Times, C letterhead-minimal, D GST-portal dense grid, E Courier cash-memo, F modern dark-band + zebra + accent total) + `bank-statement-hdfc.tsx` (HDFC-style: Date / Narration / Chq.Ref.No. / Withdrawal / Deposit / Closing Balance columns, ref extracted deterministically from the narration's own token, training-mock footer). `lib/documents/pick-template.ts` (5 tests): sha256(docType + seed) rotation, seed = `${exerciseId}:${entry.sequence}` so formats vary across documents within a batch AND across exercises while any re-render is reproducible; no Math.random in render paths. `render-source-document.ts` routes through format arrays (append-only ordering is part of the determinism contract). All 8 formats smoke-rendered successfully (distinct byte sizes confirmed real layout differences).
- `context/ui-context.md` updated with the light-default/widescreen/graduation changes.
- Gates: tsc clean, lint clean, **127/127 tests** (+5 picker), build clean. Live verification pending: fresh visitor gets light theme; chat centered at widescreen; progress shows green/keep-iterating; documents across batches show varied formats.

## Session log — 2026-08-26: PHASE 3 IMPLEMENTED (spec 15, code-complete; 122/122, build clean; NOT live-verified)

Spec `15-phase-3-help-flow-and-rules.md` implemented. The manager's verbatim Appendix A error taxonomy and full 16-title Appendix H video list were recovered from the prior session transcript, so both use his real wording, not invented approximations.

- **3-step help flow replaces the 5-rung ladder**: `determineNextRung` caps at 3 (`toHintStep` clamps legacy rung 4-5 rows to step 3); `HintSchema.rung` narrows to 1-3 for new generations while stored rows stay readable; step 1 = video-module pointer by exact title + one guiding question, step 2 = indirect hint (what needs rework, no answer), step 3 = full answer + crisp concept explanation + check question. UI: "Help step N of 3" badge, composer button now "I'm stuck, help me" / "Still stuck? Get more help". ASSUMPTION documented in hint-ladder.ts: no signal distinguishes a genuine retry from a re-click, so each help request advances one step (spec's "advance only on a genuine attempt" has no detectable input).
- **Mastery**: `CLEAN_HELP_STEP_THRESHOLD = 3` (was CLEAN_HINT_RUNG_THRESHOLD=4) — a pass that reached step 3 earns no clean credit; hint_rungs_used is the request COUNT, so legacy deep-ladder rows read as step 3 automatically.
- **`lib/llm/grounding/video-modules.ts` (new)**: all 16 Appendix H titles verbatim ("Create a company and set Books Begin Date" ... "Introduction to AIA and the AIA workflow") with conceptTags into the scored vocabulary; referenced by help step 1 and the Q&A prompt ("where do I learn this" answers name a module by exact title).
- **Educational Mode dates end to end**: `AdaptiveExerciseParams.licenseMode`; educational learners get a hard prompt rule (every date on the 1st/2nd/last day of month); `run-scoring` passes `profile.license_mode`; gate confirmed month-day-agnostic (+ test); educational walkthrough step gained the pack-dates disclosure ("post on the nearest allowed day, scoring never penalizes the date") — authored pack can't re-date, accepted limitation per spec.
- **Voucher-type-aware narration (E09)**: Payments/Receipts require a bank-reference-like token (3+ digit run or NEFT/IMPS/RTGS/UPI/UTR/cheque keyword — only when a bank ledger is on the voucher; cash vouchers exempt) AND a party mention matched against the voucher's own ledger names, including bank-abbreviation forms ("KAREMP" = Karnataka Emporium, "BNGCLEAN" = Bangalore Cleaning: shared 3-char prefix, embedded 4-char prefix, or consonant-skeleton prefix). Journals need a non-trivial why (>=15 chars, >=3 words); Sales/Purchase/Contra stay presence-only. New code `NARRATION_WEAK`. **Recalibrated against Elina's real submission**: naive party-containment flagged 23 false positives (her narrations carry parties inside UPI/NEFT reference strings, which the pilot reviewer accepted); after the abbreviation matcher, 46/50 payment-receipt narrations pass and the 4 flagged are genuine misses of the house standard (electricity payment with no bank ref, BANK CHRG, INT CREDIT, and the UNKNOWN-deposit voucher — itself one of the reviewer's real findings). Score 97.8% -> 97.5%.
- **Appendix A alignment** in `lib/schemas/scoring.ts`: `SPEC_CODE_BY_ERROR_CODE` maps every internal code to its E-code (taxonomy quoted verbatim in the comment; TDS_MISSING/TDS_UNEXPECTED = E08's two halves detected by outcome); `SPEC_CODES_WITHOUT_DETECTOR` documents E13/E14/E15, R01-R05, O01-O03 as undetectable with the reason each.
- **Gate wording verified** (spec item 6): existing messages already tell the learner exactly what to fix per build-spec Section 9.2 (sparse TB -> ledger-level re-export; out-of-period dates -> check and re-export; voucher count band; multi-part wait window handles unanswered qualitative parts). No changes needed.
- Out of scope, noted: review-exercise generation has no date instructions (anomaly-hunt over existing log, learner posts nothing new); the generated-diagnostic fallback path (non-pack) keeps natural dates — only the adaptive path (every real batch) enforces Educational Mode.
- Gates: tsc clean, lint clean, **122/122 tests** (+10: 9 narration/gate + threshold rename), build clean. Live verification pending: request help 4x on one exercise (module pointer -> indirect -> full answer -> step 3 repeats); educational learner's batch dates all 1st/2nd/last.

## Session log — 2026-08-25 (later): PHASE 2 IMPLEMENTED (spec 14, code-complete; 112/112, build clean; NOT live-verified)

Spec `14-phase-2-batch-depth.md` implemented:

- `lib/tutor/select-batch-concepts.ts` (new, pure, 5 tests): `{ strengths, weaknesses }` plan — weaknesses = primary target + concepts whose LATEST attempt failed (a later pass clears an earlier fail); strengths = mastered or clean-streak>=2, escalation excluded, never overlapping weaknesses; both sides capped at 3, deterministic alphabetical order. Note: ConceptMasteryStatus has no 'failing' value (spec text assumed one), so the failing side keys off latest-attempt results.
- Adaptive prompt: BATCH COMPOSITION block — 10 to 12 numbered transactions ("cash-to-bank alone is not a batch"), ~half STEP-UPS on strength concepts at one level above with one genuine new trap each, ~half REINFORCEMENT on weakness concepts at the stated level with scaffolding; per-transaction concept_tags must attribute each rep to its side. Escalation explicitly OVERRIDES: 2-4 focused transactions, single concept, no 50/50.
- `generateAdaptiveExercise` gained `batchPlan` (forced empty under escalation); `run-scoring` computes the plan via `selectBatchConcepts` and derives Phase 1's `recentStrengthDescriptions` from the plan's strengths (single source of truth).
- Live verification pending: next generated batch should show 10-12 transactions with both sides represented; escalation path unchanged.

## Session log — 2026-08-25: PHASE 1 IMPLEMENTED (spec 13, code-complete; 107/107, build clean; NOT live-verified)

Spec `13-phase-1-feedback-voice.md` implemented end to end:

- **Schema**: `CoachingSchema` is now `{ opening_line, went_well[], needs_work[], next_note }`; `normalizeStoredCoaching` compat mapper (in `lib/schemas/coaching.ts`, 4 tests) converts pre-change rows at the two read paths (`getFeedbackForLearner`, `getFeedbackHistoryForLearner`) — no data migration, old chats still render.
- **Prompt**: rewritten with three pilot-message excerpts as voice exemplars; opening_line MUST open with the weighted score percent (now learner-visible; qualitative sub-scores stay hidden); needs_work bullets keep specific identifiers AND state the governing principle in the same breath; rectification weaves into the sections; JSON schema updated to the new fields; hard no-em-dash output rule.
- **Guards renamed**: `checkOpeningLineFacts` / `composeFallbackOpeningLine` (fallback now includes the score, dash-free).
- **Renderer**: MessageBubble shows bold opening line, "What went well" / "What needs work" headed bullet sections, closing note.
- **Batch intro**: adaptive prompt opens by naming what the learner is strong at then what the batch targets; `generateAdaptiveExercise` gained `recentStrengthDescriptions`, computed in run-scoring from mastered concepts.
- **Em-dash sweep**: all static learner-facing strings (gate messages, action errors, ChatShell tutor notes, composer placeholders/hint label, walkthrough Educational note, Day-1 template, rectification phrasing) + no-em-dash output rules added to qa/hint/adaptive/review/source-document prompts. Verification grep over learner-facing files is clean. Accepted exception: the ledger NAME "HDFC Bank — 1234" is pack data (the company's actual account name), not tutor prose.
- **User steps**: `node scripts/seed-pack.mjs` (Day-1 template changed), restart dev, then live-verify: score-first opener, headed sections, teaching bullets, batch intro, old feedback still rendering after refresh.

## Session log — 2026-08-24 (night): manager refinement round SPECCED — four phase specs written

The user shared the manager's formal build spec (`AIA_Tally_Training_Tool_Build_Spec_v1`, extracted to `seed/manager_spec_extract.txt`), meeting notes for the manager demo, and Teams screenshots of the pilot agent's messages (the target voice — score-first opener, "What went well"/"What needs work" headed sections, teaching language). Gap analysis delivered; four phase specs written to `context/specs/` following house spec format:

- `13-phase-1-feedback-voice.md` — score % shown, headed sections, pilot voice with exemplars, batch intro names what it builds on, NO EM-DASHES in any learner-facing text (manager spec hard rule; sweep required incl. Day-1 template → reseed).
- `14-phase-2-batch-depth.md` — 10-12 transactions per batch, 50/50 split (step-up on strengths / dig into weaknesses) via new `selectBatchConcepts`; escalation stays single-concept.
- `15-phase-3-help-flow-and-rules.md` — 3-step query response replaces the 5-rung hint flow (video-module pointer → indirect hint → full answer + explanation); dummy video-module registry (spec Appendix H) NOW; Educational-Mode date rule enforced in generation; voucher-type-aware narration scoring; error-code taxonomy aligned to spec Appendix A; validation wording pass.
- `16-phase-4-ui-and-content.md` — light theme default + widescreen, concept-area graduation view (green / keep-iterating), 5-6 invoice formats + HDFC-style statement. **Variant B REMOVED from scope by user decision** — Variant A remains the only pack.
  Build order: 1 → 2 → 3 → 4. Nothing implemented yet in this round; specs only.

## Session log — 2026-08-24 (latest): re-test VERIFIED both fixes live + polish round (103/103, build clean)

Re-test confirmed both prior fixes working live: feedback now names real identifiers ("GST treatment for INV-012, INV-001, DN-M1", "SA-101", "the Prepaid Software journal" — matching the pilot reviewer's findings) and Batch 2 stayed in-company (HDFC Bank — 1234, Karnataka Emporium, Bangalore Cleaning, 06-May-2026, contra target). Rectification's "recurring failures" note fired correctly on the repeat submission. Three residual blemishes fixed:

1. **Bare "transaction 78/94" labels** — sequences whose every leg is a generic account (Bank Charges, Suspense) got no label; `buildSequenceLabels` now falls back to the most descriptive leg, preferring non-bank/cash ("the Suspense receipt" over "the HDFC Bank — 1234 receipt").
2. **Batch prompt polish** — real-ledgers-only rule (no invented "Wallet" holding accounts; cash moves through actual Cash/bank ledgers) + spread transactions across different dates in the month.
3. **Source-document party invention** ("Invoice — Shree Traders.pdf" for a party in no transaction) — root cause: the doc prompt said "include a plausible vendor name" with zero party grounding. `generateSourceDocument` now receives the transaction's sibling leg accounts and the prompt pins the document's vendor/customer/account-holder name to that list, never invented. Prompt-level fix — verify on the next generated batch with documents.

## Session log — 2026-08-24 (later): full-flow live test findings, 2 root causes fixed (103/103, build clean)

User ran the complete Day-1 flow from scratch (reset → walkthrough → pack → Elina XMLs → feedback → Batch 2). Pipeline worked end-to-end; three issues surfaced in the output, diagnosed and resolved:

1. **Batch 2 invented a fictional company** (Maharashtra Savings/Current accounts) — root cause: the PACK diagnostic path never seeded `company_ledger_registry`/`company_transaction_log` (only `generateAdaptiveExercise` did), so the batch generator was told the company was empty and to "introduce names freely". Fix: `assign-pack-exercise.ts` now registers every answer-key account + logs a pack-month summary (with company name) at assignment; adaptive prompt opens with a COMPANY CONTINUITY IS MANDATORY block.
2. **Feedback went fully generic** ("the relevant transactions") — root cause: the coaching signal only ever carried bare sequence numbers ("transaction 43"), meaningless for pack exercises whose transactions live in files; the earlier specific-sounding run was the model recalling detail by luck. Fix: `buildSequenceLabels` (exported, tested) names each sequence by bill reference ("INV-012") or distinctive party/expense account ("the Advertisement & Marketing purchase"); `generateCoaching` takes optional `answerKey` (passed by both jobs); prompt now forbids vaguer-than-signal phrasing. Unlabeled path (generated drills) keeps the original numeric phrasing byte-for-byte.
3. **"Partial due to Trial Balance mismatch"** — NOT a bug: tie-out genuinely fails on this submission (real errors: gross bank charge, Signage residual), and the result-line guard only blocks TB-blame when tie-out passed. Confirmed working as designed.

## Session log — 2026-08-24: PERSISTENT CHAT HISTORY + GPT-STYLE COMPOSER (102/102, build clean)

User decision: the chat must behave like GPT/Claude — (1) refresh never loses the conversation, (2) one paperclip/text/send composer with conversational file handling. Same engine underneath, new interaction layer.

- **Migration `20260824120000_chat_history.sql` (NOT applied yet)**: `qa_messages` (Q&A exchanges were stored nowhere; RLS select/insert own) + `submissions.daybook_filename`/`trialbalance_filename` (original upload names for history chips).
- **History rebuild**: `lib/chat/build-timeline.ts` — `assembleTimeline` (pure, 3 tests) + `buildChatTimeline` (authenticated client, RLS-scoped) reassemble every exercise/submission/feedback/hint/Q&A into one ordered timeline on every page load; ChatShell opens with it and appends live events. Messages are DERIVED from the same rows that drive scoring, so history can't drift. New list queries: exercises/submissions/feedback-history/hints per learner; `getFeedbackHistoryForLearner` selects learner-facing fields only (same boundary as getFeedbackForLearner). Historical exercise labels use the current module number (historical module values aren't stored — cosmetic).
- **GPT-style composer**: Composer.tsx rewritten — 📎 multi-attach + one text box + one Send; chips removable; ChatShell's `handleSend` routes conversationally: 1 file → tutor asks for the second (files stay attached); 3+ → asks once, second Send proceeds; 2 (or confirmed extra) → submit, and any text alongside becomes a Q&A answered after; server errors render as tutor bubbles, not red text. `submitFiles` now takes generic `files[]` and classifies Day Book vs Trial Balance by CONTENT (`lib/parsing/identify-tally-file.ts` — parse-attempt, filename never consulted); original filenames persisted. `askQuestion` persists exchanges (non-fatal on insert failure).
- **User steps**: apply the migration, restart dev, then test: refresh mid-conversation (history must survive), attach 1 file → nudge, attach 3 → confirm flow, ask a question → refresh → Q&A still there.

## Session log — 2026-08-21: FIRST FULL LIVE RUN of the hybrid + latency fixes (99/99, build clean)

**The hybrid worked live, end to end**: real pilot XMLs → gate pass (99v99) → engine → `adjudicate-findings` (44s) → coaching → feedback rendered ("Partial", TB-mismatch cause — factually correct, tie-out genuinely failed on the learner's real errors) → Batch 2 auto-appended with 4 generated source-document PDFs. Total ~3 minutes, which surfaced three issues, all fixed:

1. **Langfuse blocked every LLM call** — `await langfuse.flushAsync()` per call against an unreachable host meant each of the run's ~8 LLM calls waited out a ~10s network timeout. Now: fire-and-forget flush, `requestTimeout: 3000`, `fetchRetryCount: 0`, disabled when keys are absent.
2. **Feedback held hostage by batch generation** — the mark-scored-last design (2026-08-19's race fix) delayed feedback until the next batch + PDFs finished (~90s extra). Reverted: 'scored' flips inside `persist-scoring-result`; `PendingSubmission` shows feedback immediately and POLLS `getNextExercise` (5s interval, 5-minute budget, loop deliberately survives its own unmount) — fixing the original race properly without the latency.
3. **Batch 2 generated 2023 dates** — the validity gate would reject those (books begin 01-Apr-2026). Adaptive prompt now pins dates: 2026, continuing the company timeline, INR; wrong year called out as gate-fatal in the prompt. NOTE: the adaptive exercise generated during this run still carries 2023 dates in the DB — delete it (`delete from exercises where kind = 'adaptive';`) and resubmit to regenerate correctly.

## Session log — 2026-08-20 (later): HYBRID SCORING — LLM adjudication over engine findings (99/99 tests, build clean)

User decision (after weighing pure-deterministic vs pure-LLM vs hybrid): **hybrid — the engine FINDS, the LLM JUDGES.** Clarified intent: the program design is intern-general, not fitted to the pilot trainee — her submission was regression data, and the adjudicator now replaces hand-authored tolerance (aliases etc.) as the general mechanism for accepting valid practice variations.

- `lib/schemas/adjudication.ts` — per-finding verdict schema (sequence, field, uphold/dismiss, internal reason).
- `lib/llm/prompts/adjudication.ts` — judge brief: strict on substance (GST regime/side, TDS base/thresholds, amounts, credit-vs-cash routing, missing refs, sloppy Suspense), tolerant on form (naming, valid structural alternatives, ref formats, sub-rupee rounding); grounded in the real Rulebook; unsure → uphold. Runs server-side only (answer-key boundary same as hints).
- `lib/tutor/adjudicate-findings.ts` — orchestration (bounded 3-retry + Zod) + pure `applyAdjudicationVerdicts` (exported, 4 tests). **Fail-safe by construction**: missing verdict, validation exhaustion, or thrown error → engine verdicts stand; adjudication can only relax findings, never invent them. Skips when no findings, and when >50 transactions are flagged (a that-broken submission isn't a rigidity problem).
- `score-submission.ts` — exported `rebuildScoringResult` (dismissals rebuild weighted score/overall/concepts through the one scoring path) + `matchVouchersToTransactions`.
- Wired into BOTH jobs: `run-scoring.ts` (new `adjudicate-findings` step between score-submission and the mastery/coaching consumers) and `wait-for-submission.ts` (inside its scoring step). `CallType` + `'finding-adjudication'`; architecture.md LLM table updated.
- NOT live-verified: needs a real submission run (the adjudication call itself is only exercised live, per this codebase's convention for generation code).

## Session log — 2026-08-20: PILOT CALIBRATION — engine scored against the real pilot submission (95/95 tests, build clean)

The user revealed their uploaded XMLs were the pilot trainee's ACTUAL diagnostic submission (now at `xmls/pilot-submission/elina-daybook.xml` + `elina-trialbal.xml`; the repo's original test fixtures they overwrote were reconstructed faithfully to every test assertion). The pilot reviewer scored this submission 173/200 (86.5%) with specific findings. Built `scripts/calibrate-pilot.mjs` and iterated the engine against that ground truth: **43.9% → 97.8% weighted, 98→12 flagged transactions, and the surviving findings match the pilot review nearly 1:1** (INV-012 IGST slip, CN-001 Input-vs-Output GST, gross bank charge, UNKNOWN-vs-Suspense, Signage TDS-on-gross residual, INV-18 ref typo, missing early refs).

Engine bugs found and fixed (each was systematic, not cosmetic):

1. **Parser dropped every accounting-mode voucher's legs** — Tally puts Payment/Receipt/Journal/Contra legs under `ALLLEDGERENTRIES.LIST`, unsupported until now; all ~50 of the pilot's bank vouchers parsed half-empty. Also **real bill allocations were never parsed**: Tally repeats `<BILLALLOCATIONS.LIST>` per allocation with NAME/AMOUNT as direct children — the parser expected an inner wrapper that only existed in this repo's hand-built fixtures.
2. **TB parser: signed single-amount format** (`DSPCLAMT/DSPCLAMTA`, negative=Dr) — the pilot's TB parsed as all-zero balances.
3. **Account matching**: normalized + containment (≥5 chars) + typo tolerance (Levenshtein ≤2 at ≥8 chars, ≤1 at ≥6 — the pilot file really contains "Elecrticity Charges") + `account_aliases` on AnswerKeyEntrySchema (register-nature naming like "Trading goods", netted returns into "Sales", etc. — all accepted by the pilot reviewer). TB tie-out aggregates ALL alias-matching rows (learner may split "Sales" into Cash/Credit Sales A/c).
4. **Own-leg tax false positives**: "unexpected GST/TDS" now ignores entries that ARE the transaction's expected legs (TDS-deposit voucher, GST set-off JV). Tax-named legs are amount-exempt (cascade-proof) — upstream slips legitimately change every set-off figure.
5. **Bill references**: any-allocation vs any-expected-ref matching (normalized containment).
6. **Proportional concept rollup** (`CONCEPT_PASS_RATIO = 0.9`) — identical on 1-3-txn drills, fair on ~100-voucher packs.
7. **Pack gate tolerance** (±10%): the pilot's 99-voucher daybook vs the 98-voucher key was scored by the reviewer, not bounced — now ours too. Exact check retained for generated drills.
8. **Key correction to house practice**: CARESW prepaid modeled as payment + separate month-end apportionment JV (per the pilot's own KT doc), making the key 99 vouchers — matching the pilot submission exactly. TDS deposit needs no bill ref.
   Also this session: 25 MB Server Action body limit (real Tally exports are several MB; 1 MB default rejected them), spinner poll safety-net in PendingSubmission (Realtime gap observed live), any-variant pack fallback in assign-pack-exercise.

Remaining intentional divergences from the pilot review: narration CONTENT quality (bank-ref+party standard) is not judged — presence only; the reviewer's 86.5% is a holistic rubric vs our per-field weighting, so absolute percentages differ while findings align. **User steps to go live with the calibrated key: re-run `node scripts/seed-pack.mjs` (key changed), `delete from exercises;` (old exercise carries the old key — invariant 6), reload /chat → Start my training → re-upload.**

## Session log — 2026-08-19 (latest): full fix-and-complete pass (code-complete, NOT verified live; 90/90 tests, build clean)

User instructed: fix all known issues + pending buildable work through Unit-12 scope, skip AIA. Done in this pass:

**Scoring-engine bugs found by deep review and fixed** (all with tests):

1. **TB tie-out ignored opening balances** — would have failed EVERY pack submission (pack company opens with ₹11L of balances). `AnswerKeySchema` gained optional `opening_balances`; tie-out seeds closings with them (closing = opening + movements); derivation script emits the pack's 11 opening rows.
2. **TB tie-out compared GST/TDS ledgers it can't know** — the leg model carries GST/TDS as metadata, not legs, so Output/Input GST + TDS Payable balances are untrackable; those account names (/gst|tds/i) are now tie-out-exempt (still scored per-voucher), and a fully-settled account absent from the learner's TB only fails on a NON-zero expected closing.
3. **Intra-state GST false errors** — expected CGST vs inferred SGST (ledger order chance) scored GST_HEAD_WRONG at 2x weight; CGST/SGST now equivalent (regime check: IGST-vs-pair still wrong).
4. **Narration was unscoreable** — parser now extracts NARRATION (`VoucherSchema.narration`); scorer checks presence (content-matching deliberately not attempted). Clean pass is now genuinely 1.0, not capped at 0.9; error-code/concept tests updated accordingly.
5. **Positional voucher matching** — voucher N vs transaction N breaks on same-day reordering (cascades across the 98-voucher pack); replaced with greedy similarity matching (account-name +4/leg, amount +1, voucher type +1; ties keep daybook order; positional fallback preserves VOUCHER_MISSING).

**Known issues fixed:** 6. **result_line TB mis-attribution, structural** — `checkResultLineFacts` (exported, tested) treats a Trial-Balance-blaming result_line as a validation failure when tie-out matched → retry; on exhaustion `composeFallbackResultLine` replaces it with a deterministic line. Prompt-only mitigation observed insufficient live. 7. **Blank-chat dead end** — `confirmWalkthrough` is now self-healing (creates the diagnostic when none exists, regardless of walkthrough flag; existence-check remains the duplicate guard) + ChatShell renders a "Start my training" recovery button on the empty state. 8. **No Q&A during explain/review exercises** — Composer shows "Ask instead" beside "Send answer" when the text input is a submission part.

**Pending work completed:** 9. **Explain/review scheduling** — Unit 11's generation was unreachable; `select-exercise-kind.ts` (pure, tested) now schedules: every 3rd exercise 'explain', every 5th 'review' (needs ≥4 company-log entries; failed review generation falls back to adaptive). **ASSUMPTION on cadence flagged for user confirmation** — mirrors the pilot arc, easily retuned. `generateAdaptiveExercise` gained a kind param; wired in `run-scoring.ts`'s generate-next-exercise; `countExercisesForLearner` added. 10. **Module-doc grounding** — all 8 training .docx extracted to `lib/llm/grounding/module-docs.ts` (~40k chars) with `CONCEPT_TO_MODULE_DOC`; hint rung 2 now points at real module docs (was "placeholder video slot"); Q&A grounds in Rulebook + all module docs. 11. Answer key regenerated: 98 vouchers / 202 legs (head-wise GST set-off JV replacing aggregate legs, since aggregate "Output GST" could never match learner books) + 11 opening balances.

**Still user-only:** apply `20260819120000_exercise_packs.sql`, `node scripts/seed-pack.mjs`, answer-key accounting review, live click-through, RLS spot-checks. **Still deferred:** AIA (user instruction), capstone/certificate (beyond Unit-12 scope), Variant B (files not provided), Unit 12's remaining live-verify items.

## Session log — 2026-08-19 (later): Units 14R + 15R implemented (code-complete, NOT yet verified live)

Built in one push on the user's "full execute" instruction; `tsc`/lint/`npm test` (78/78, up from 76) /`npm run build` all clean. **Nothing verified live yet** — see the run-steps below.

- **Answer key derived, needs user review before trusting scores**: `scripts/derive-blossom-answer-key.py` reads the pack xlsx and emits `seed/blossom-variant-a/answer_key.json` (98 vouchers / 198 legs) + `answer_key_review.md` (every authored judgment call flagged: partial-payment allocations, ADV-01/ADV-02 advances, Mumbai 4,50,000 split B1/B2/B3-partial, Signage 216 residual, 850 gross bank-charge split 720.34+129.66, month-end rent provision + GST set-off JV computed as Output 4,34,340 vs Input 2,75,629.66 → net payable 1,58,710.34). Validated against the real Zod schema by `lib/tutor/pack-answer-key.test.ts`. Known engine limitations documented in the review doc: position-based voucher matching (same-day ordering must match register order), single-head GST check (intra-state keyed CGST), narration presence-only.
- **Migration `20260819120000_exercise_packs.sql` (NOT applied)**: `exercise_packs` (server-only, no authenticated policies), `exercises.expected_voucher_count`, `learner_profile.full_name`, shared `packs` Storage bucket (authenticated select, service-role write).
- **Seed**: `scripts/seed-pack.mjs` uploads the 4 xlsx + upserts the Variant A pack row with the generalized Day-1 message (Shruti's, `{{name}}` template; AI-tools note and ask-here note included; Books Begin 1-Apr-2026 — NOTE: pack's Company Master says books begin 1-Jan-2026, flagged to user, template follows the pilot chat's 1-Apr instruction).
- **Pack diagnostic flow**: `assign-pack-exercise.ts` (variant by learner hash → pack fetch → `{{name}}` render → `insertPackExercise` copying the key, invariant 6 preserved), `confirmWalkthrough` tries pack first and falls back to LLM generation if unseeded; pack file download cards served via `getSignedPackFileCards` ('packs' bucket) on both the action and page-load paths; validity gate now uses `expectedVoucherCount ?? transactions.length` (test added).
- **Free-form Q&A (Unit 15R)**: `lib/schemas/qa.ts`, `lib/llm/prompts/qa.ts` (grounded in the REAL Rulebook; two-mode rule: general questions answered directly like the pilot's reviewers, current-exercise transactions never solved — hint ladder owns that), `lib/tutor/answer-question.ts` (bounded retry + Zod), `askQuestion` action, `qa-question`/`qa-answer` message kinds, composer text input now always live (submission part when the exercise needs one, tutor question otherwise), `CallType` + `'qa-response'`.
- **Real Rulebook grounding (Unit 13R, folded in)**: `lib/llm/grounding/rulebook.ts` (24.8k chars extracted from the v0.2 docx) replaces the placeholder in the coaching prompt; Q&A uses it too. Module-doc extraction not done yet (docx set exists in Reference Material).
- **Shruti-style prompts**: coaching voice restyled (what-went-well/needs-work reviewer voice); adaptive generation restyled to the pilot's numbered "batch" format (dates, states, explicit amounts, deliverables line, narration standard reminder).
- **Onboarding**: name field added (form + schema + action + `completeOnboarding`); existing accounts have `full_name` null → greeting falls back to "there".
- **Run-steps to go live (user)**: (1) apply `20260819120000_exercise_packs.sql`; (2) review `seed/blossom-variant-a/answer_key_review.md`; (3) `python scripts/derive-blossom-answer-key.py` if pack files changed, then `node scripts/seed-pack.mjs`; (4) reset test learner (or new signup) and click through Day-1; (5) RLS checks on `exercise_packs` (authenticated select must FAIL) + packs bucket (authenticated select succeeds, insert fails).

## Session log — 2026-08-19 (testing session, bugs found & fixed live)

End-to-end testing by the user (as a developer without Tally, using hand-built XML fixtures) surfaced and fixed seven issues. All verified live by the user in-browser except where noted; build/lint/test clean throughout (76/76 Vitest at session end, up from 66):

1. **Submission-id mismatch (RLS 42501)** — `submitFiles` generated a UUID for Storage paths/`submission_parts` but `insertSubmission` let Postgres generate a different row id, so parts pointed at a nonexistent submission and RLS correctly rejected the insert. Fix: `insertSubmission` now takes the caller's id explicitly (`lib/db/queries/submissions.ts`, both call sites in `app/(chat)/chat/actions.ts`). Pre-fix rows have orphaned Storage files under a different UUID than the row id.
2. **Missed Realtime terminal status (infinite spinner)** — the Inngest job could finish before the client's Postgres Changes subscription opened; the transition event was lost forever. Fix: new `getSubmissionStatus` Server Action + catch-up read on `PendingSubmission` mount, sharing an idempotent `resolveTerminalStatus` with the subscription path.
3. **Coaching hallucinated a missing explanation part** — the system prompt carried the missing-part example sentence on every call and said nothing when the submission was complete; the model invented "your explanation never came through" on a 2-part exercise. Fix (`lib/llm/prompts/coaching.ts`): removed the always-present example, added an explicit only-what-the-signal-says rule, and the user message now states completeness positively. Mitigation, not guarantee — free-text fields can still carry false statements past Zod.
4. **Praise for untested GST/TDS** — scorer emits `is_correct: true` for "expected nothing, got nothing" GST/TDS diffs (correct for the weighted score), and coaching praised them. Fix: `vacuously_correct` marker on `VoucherDiffSchema`, set in `score-submission.ts`'s two null-expectation branches, filtered out of praise in `generate-coaching.ts`. Score math unchanged (test-asserted). 3 new tests.
5. **Flagged-areas dump (14 near-identical bullets)** — one description per incorrect per-voucher diff. Fix: `groupDescriptionsByField` in `generate-coaching.ts` (exported for tests) groups by field with transactions named inside one entry, capped at 4 areas prioritized by field weight (GST/TDS first); prompt instructs exactly one entry per listed area. 7 new tests (`generate-coaching.test.ts`). Verified live: 4 grouped bullets. **Known remaining issue: `result_line` still occasionally mis-attributes failure to the Trial Balance when tie-out passed** — prompt instruction added but not reliable; structural fix (code-derived result line, or post-generation validation against `tb_tie_out`) proposed, not yet built.
6. **Next exercise never appeared (status raced generation)** — `persist-scoring-result` set `status='scored'` before `generate-next-exercise` ran, so the client's `getNextExercise` fetch found nothing; the exercise landed invisibly and the validity gate then checked new submissions against it ("expects 1 transaction, got 4" confusion). Fix: status update moved to a new final `mark-scored` step in `run-scoring.ts` — any future step must go above it. (`wait-for-submission.ts` unchanged: no next-exercise step, no race.)
7. **Chat wiped on next exercise** — `handleNextExercise` cleared the timeline and `messages` re-derived from current exercise only, so feedback vanished when the next exercise auto-delivered. Fix: `ChatShell.tsx` timeline is now append-only; new exercises append as messages (walkthrough diagnostic path included); `exercise` state now only tracks what the composer/hints act on. Verified live: exercise → submission → feedback → next exercise all visible in order.

Also this session: `20260817130000_module_progress.sql` applied live by the user (was outstanding from Unit 12); learner data reset (keeping `learner_profile`, `anomaly_templates`) via reviewed SQL; **known UX gap, flagged not fixed**: a learner with `walkthrough_completed_at` set but zero exercises gets a blank chat with no recovery path (`/chat` only generates via the walkthrough confirm action — recovered manually by nulling `walkthrough_completed_at`). **MCP warning reconfirmed**: the connected "Supabase VA" MCP points at an unrelated project (clients/invoices/vas tables) — verified by table listing; never run writes through it for this app.

## Completed

- **Unit 12 — Progress view + rectification tracking** (spec: `context/specs/12-progress-view-rectification.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 66/66 passing, up from 56 — 15 new tests: `rectification.test.ts` new (9 tests), `module-progress.test.ts` new (6 tests)) all pass clean. Code-complete and self-verified (build/lint/test); **not yet verified live by the user** — no migration apply, no RLS direct test, no live click-through confirmed yet. Do not mark this unit done until the user confirms the spec's verify checklist.
  - **Module-advancement assumption, flagged and confirmed with the user before writing code**, per `ai-workflow-rules.md` rule 14 and the spec's own explicit "confirm this against the original build spec before relying on it" instruction: checked `context/specs/00-build.md` first — it does not resolve the rule either (Unit 12's build-order entry only lists "module progression table" as a deliverable, no advancement rule). Asked the user directly; confirmed to proceed with the spec's own stated assumption: a learner advances to the next module once every concept tagged to their `current_module` has reached `concept_mastery.status === 'mastered'` with no active escalation on any of those concepts.
  - **Second ambiguity flagged and resolved with the user before writing code**: no concept→module grouping exists anywhere in the codebase (Unit 09 deliberately made "module number" a _derived_ progress-label value — mastered-count + 1 — not a stored grouping). This unit's `module_progress.current_module` is a genuinely different, stored concept that needs a real grouping to evaluate advancement against. User confirmed: one concept per module, in `CONCEPT_TAGS` declaration order (module 1 = `sales_voucher_basics` ... module 11 = `trial_balance_tie_out`) — simplest possible static map, no new thematic judgment calls invented.
  - **Data model** (new migration `supabase/migrations/20260817130000_module_progress.sql`, **not yet applied to the live Supabase project**): `module_progress` (`learner_id` primary key references `auth.users`, `current_module` int default 1, `current_level` int default 0, `updated_at`), RLS with only a `select` policy scoped to `auth.uid() = learner_id` — no insert/update policy for `authenticated`, same invariant-5 discipline as `concept_mastery` (all writes go through the service-role client, from exactly one call site).
  - `lib/schemas/exercise.ts` — added `CONCEPT_TO_MODULE` (a `Record<ConceptTag, number>` derived from `CONCEPT_TAGS`'s declaration order), the concept-to-module lookup confirmed above. Cross-boundary touch into a prior unit's file, same pattern as Units 06/09/10/11's own extensions of this file.
  - `lib/tutor/module-progress.ts` — new file, `deriveNextModuleProgress`: pure function, no DB write (same discipline as `mastery.ts`) — takes the current `module_progress` row (or `null`, defaulting to module 1/level 0) and the learner's full `concept_mastery` map, filters `CONCEPT_TO_MODULE` down to concepts tagged to the current module, and advances (`current_module + 1`, `current_level` reset to 0) only if every one of those concepts is `'mastered'` with `escalation_active: false`. A module with no concepts mapped to it (past the last defined module) is a no-op, not an error.
  - `lib/tutor/rectification.ts` — new file, `classifyRectification`/`classifyRectificationsForExercise`: pure functions, no LLM call, per the spec's explicit requirement. Given a concept and its full `concept_attempts` history (sorted oldest-first), classifies the _latest_ attempt against the one immediately prior: `FIXED` (prior failed, latest passed), `STILL_FAILING` (prior failed, latest failed again), `NEW` (no prior attempt at all, and this one failed), or no classification (first-ever attempt passed, or a pass/fail with no prior _failure_ immediately before it — steady progress or a fresh miss, neither is a rectification event worth calling out, matching the spec's explicit "don't spam every passing result" requirement).
  - `lib/db/queries/module-progress.ts` — new file: `getModuleProgress`, `upsertModuleProgress` (the one sanctioned write path, upserts on `learner_id` — confirmed by grep that no other call site exists anywhere in the codebase).
  - **Coaching integration** (per spec: "feed this into Unit 06's `generate-coaching.ts` as additional structured input, not a new LLM call"): `lib/llm/prompts/coaching.ts`'s `CoachingSignal` gained `rectificationDescriptions: string[]`; the system prompt now instructs the model to weave each rectification note into `praise` (for FIXED) or `flagged_areas` (for STILL_FAILING) as a short plain line within the existing feedback bubble — explicitly told not to invent a new section, matching the spec's "no new bubble type" instruction. `lib/tutor/generate-coaching.ts`'s `generateCoaching` gained an optional `rectificationDescriptions` param, defaulted to `[]` everywhere it isn't passed (so `wait-for-submission.ts`'s existing call site, untouched by this unit, keeps working with no rectification notes — see scoping note below).
  - **`lib/jobs/run-scoring.ts` reordered**: Unit 09 originally logged `concept_attempts` and recomputed mastery _after_ `generate-coaching` ran. Rectification needs this exercise's just-logged attempts (classified against their immediately-prior history) as coaching _input_, so the concept-attempt logging + rectification classification step (`log-attempts-and-classify-rectification`) now runs _before_ `generate-coaching`, and the mastery-patch application was consolidated with the new module-progress evaluation into one step (`recompute-mastery-and-module-progress`) that now runs after `persist-scoring-result`, unchanged in effect from Unit 09's original ordering relative to `generate-next-exercise`. No behavior of Unit 09's mastery/reinforcement/escalation logic changed — only where concept-attempt logging happens relative to coaching, and the addition of the module-progress step alongside it.
  - **Scoping note, confirmed by code review**: `lib/jobs/wait-for-submission.ts` (Unit 11's multi-part job) never called mastery recompute at all, even before this unit — it has no `concept_attempts`/`concept_mastery` step of its own, a pre-existing gap unrelated to this spec. Per the spec's explicit instruction ("invoked as an additional step in Unit 09's mastery recompute"), module-progress/rectification wiring was added only to `run-scoring.ts`, where that recompute step actually exists — extending `wait-for-submission.ts` to also recompute mastery was out of scope for this unit and not requested.
  - **Progress page**: `app/(progress)/progress/page.tsx` (Server Component, same auth+onboarding-gate pattern as `dashboard`/`onboarding`/`chat` pages) — fetches `module_progress` and the full `concept_mastery` map, renders "Module N · Level L" prominently at the top, then `CONCEPT_TO_MODULE`-grouped sections (one per module number, in order) listing every concept in that module with its status badge. `app/(progress)/progress/ConceptStatusBadge.tsx` (new, colocated presentational component per code-standards rule 21) — mastered renders the `status-success` bg-tint/text badge, escalation-active (and not mastered) renders the `status-warning` bg-tint/text badge, everything else renders a plain `text-muted` "Developing" label — composed directly from the existing `status-success`/`status-warning` Tailwind colors (same compositional pattern `MessageBubble.tsx`'s `RESULT_BADGE_STYLES` already uses), rather than adding new `mastery-badge`/`escalation-badge` token names to `tailwind.config.ts` that nothing else in the codebase defines as distinct CSS variables — `ui-context.md`'s token table itself describes them as exactly this bg-tint/text composition, not a separate color value. `proxy.ts` — `/progress` added to `PROTECTED_PATHS`.
  - Dependencies: none new — built entirely on Units 06/09's existing coaching/mastery infrastructure, per spec.
  - New test files (colocated per code-standards rule 22): `lib/tutor/rectification.test.ts` (FIXED/STILL_FAILING/NEW classification, the no-spam-on-steady-progress case, order-independence, multi-concept exercise classification — 9 tests), `lib/tutor/module-progress.test.ts` (advancement, blocked-by-not-mastered, blocked-by-escalation, no-row defaults, no-op past the last module, unaffected-by-other-modules — 6 tests).
  - **Environment note**: the full Vitest suite intermittently fails all files with `TypeError: Cannot read properties of undefined (reading 'config')` due to a pre-existing Node/Vitest ESM-vs-CommonJS warning destabilizing the run (`vitest.config.ts` uses ESM syntax but `package.json` has no `"type": "module"`) — unrelated to this unit's code (reproduces identically on pre-existing test files). Workaround confirmed: `VITE_CONFIG_NATIVE_IGNORE_WARNING=true npm test` runs clean. Not fixed as part of this unit (out of scope, pre-existing, not something this spec touches) — flagging here in case it recurs for the user or a later unit.
  - **Outstanding before this unit can be marked done** (per `ai-workflow-rules.md` Section 7 — none of these have been done yet, only self-verified via build/lint/test):
    - [ ] Apply `20260817130000_module_progress.sql` to the live Supabase project
    - [ ] RLS direct test on `module_progress` (cross-learner select denial)
    - [ ] Live click-through: a concept that failed once and passes cleanly next attempt is classified FIXED in the actual generated feedback message
    - [ ] Live click-through: a concept that fails twice in a row is classified STILL FAILING in actual generated feedback
    - [ ] Live click-through: a concept's first-ever failed attempt is classified NEW (confirmed via code review only so far — `NEW` currently has no coaching-signal wiring since only FIXED/STILL_FAILING are described to the prompt, matching the spec's "NEW needs no special callout beyond normal flagged-area treatment" instruction — worth the user confirming this reads correctly live, not just correct in code)
    - [ ] Confirm live that a clean back-to-back pass produces no rectification spam
    - [ ] Simulate a full module's concepts reaching `'mastered'` with no escalation and confirm `module_progress.current_module` actually advances live
    - [ ] `/progress` renders correctly against real data, in both light and dark mode, matching `ui-context.md` tokens
    - [ ] Confirm `module_progress` is only ever written through `upsertModuleProgress` (grepped clean during this session — worth a final check after any future change)

- **Unit 11 — Multi-part submissions + qualitative scoring + anomaly seeding** (spec: `context/specs/11-multipart-qualitative-scoring.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 51/51 passing, up from 40 — 11 new tests across `submission-parts.test.ts` and `score-qualitative.test.ts`) all pass clean. Fully verified working end-to-end by user against the spec's complete checklist: migration applied to the live Supabase project, `anomaly_templates` seeded, RLS confirmed by direct test on all touched/new tables, and the full live click-through (out-of-order multi-part timing, window-expiry behavior, qualitative scoring output, live status checklist UI) confirmed.
  - **Two ambiguities flagged and resolved with the user before writing code**, per `ai-workflow-rules.md` Section 4:
    1. The spec's own flagged open assumption (review-packet-in-chat vs. live-company export): confirmed **review-packet-in-chat**, as the spec builds by default. `context/specs/00-build.md` is silent on the mechanics and doesn't resolve it either, so this was a genuine open question, not something derivable from existing docs.
    2. `exercises.kind` was only `'diagnostic' | 'adaptive'` and `AnswerKeyEntrySchema` is built entirely around double-entry Tally postings — neither had a shape for an explain-the-entry or ledger-review exercise. User confirmed: add `kind` values `'explain'` (direct-entry posting + `explain_text`, same `answer_key` shape as before) and `'review'` (no Tally posting at all — `ledger_review_items` is this kind's answer-key equivalent, `answer_key.entries` persists empty). A follow-up question confirmed `explain_text` is graded against the exercise's own `answer_key` (reused as qualitative grounding, not a separate hand-authored explanation key) — user's choice, since `answer_key` already carries what the explanation needs to be graded against.
  - **A third design fork surfaced mid-implementation and resolved with the user**: `scoring_results.weighted_score`/`tb_tie_out` are `NOT NULL`, sized for the quantitative Tally-posting score — a `'review'` exercise has no Tally posting at all, only a qualitative score. User confirmed: add a nullable `qualitative_score` jsonb column alongside the existing quantitative columns rather than a separate parallel table; for a review exercise, `weighted_score`/`tb_tie_out` are persisted as `0`/`true` purely to satisfy the existing constraints, documented at every write site, and `overall_result` is derived from `qualitative_score` alone via the new `combineOverallResult` (`lib/tutor/score-qualitative.ts`) — takes the worse of the quantitative and qualitative results when both apply (an explain exercise), so a clean voucher posting can't mask a weak explanation and vice versa.
  - **Data model** (new migration `supabase/migrations/20260817120000_multipart_qualitative_scoring.sql`, applied to the live Supabase project by the user):
    - `exercises.kind` check constraint extended to `'diagnostic' | 'adaptive' | 'explain' | 'review'`; new `required_parts text[]` column (default `{daybook_xml,trialbalance_xml}`, unchanged for diagnostic/adaptive).
    - `submission_parts` (`id`, `submission_id`, `part_type` check-constrained to the four part types, `content` jsonb, `received_at`, unique on `(submission_id, part_type)`) — RLS `select`/`insert` policies scoped via a join through `submissions.learner_id` (this table has no `learner_id` column of its own). Unlike `exercises.answer_key`/`scoring_results`, this table needs an **insert** policy for `authenticated` (not service-role-only), since learners post `explain_text`/`review_text` themselves as ordinary chat messages and the authenticated client inserts the file-part rows too, matching how `submissions` itself is already learner-insertable. Added to the `supabase_realtime` publication for the live status checklist.
    - `anomaly_templates` (`id`, `concept_tag`, `anomaly_description` (server-only), `clean_distractor_description`, `difficulty_level`) — **no RLS policies at all**, not even select for `authenticated`; this is server-only library content read only via the service-role client, same discipline as `exercises.answer_key`. Seeded by the user with real data.
    - `ledger_review_items` (`id`, `exercise_id`, `source` (`'real_transaction' | 'generated_distractor'`), `company_transaction_log_id` (nullable FK), `anomaly_template_id` (nullable FK), `is_anomaly`, `presented_text`) — this is the review exercise's answer-key equivalent; RLS select-only via a join through `exercises.learner_id`, no insert/update policy for `authenticated`, same pattern as `exercise_source_documents` (Unit 10).
    - `submissions.daybook_path`/`trialbalance_path` changed from `NOT NULL` to nullable — a `'review'` exercise's submission has no file parts at all. `submission_parts` is now the source of truth for what actually arrived; these two columns remain populated for exercises that do require file parts, unchanged from Units 05-07.
    - `scoring_results.qualitative_score` (nullable jsonb) added, per the third design fork above.
    - Minor cross-boundary touch into Unit 09's `company.ts`: added an `id` field to `CompanyTransactionLogEntry` (was previously omitted from the select) — needed so `ledger_review_items.company_transaction_log_id` can reference a specific log row when a review packet includes a real transaction.
  - **Qualitative scoring**: `lib/schemas/qualitative-scoring.ts` — `QualitativeScoringSchema` (`recall`/`precision`/`reasoning_quality` 0-100, `rationale` internal-only), matching the spec's schema exactly. `lib/llm/prompts/qualitative-scoring.ts` + `lib/tutor/score-qualitative.ts` — genuinely calls the LLM (unlike Unit 06's deterministic `score-submission.ts`), grounded tightly against a server-only issue list passed in by the caller (never the learner-facing packet/scenario text alone), same bounded-3-attempt-retry + Zod-validation discipline as every other LLM call in this codebase. `lib/llm/tracing.ts`'s `CallType` extended with `'qualitative-scoring'` and `'review-exercise-generation'`.
  - **Plain-language framing, never raw numbers**: `lib/tutor/generate-coaching.ts`'s `describeQualitativeSubscore` buckets each 0-100 subscore into strong/mixed/weak and hands the coaching LLM a plain-English description ("caught nearly all of the real issues") — the coaching prompt (`lib/llm/prompts/coaching.ts`) is explicitly instructed to weave this into `praise`/`flagged_areas` and never state a number or the word "score." `generateCoaching`'s signature changed to accept a combined `{overallResult, scoringResult, qualitative, missingPartDescriptions}` params object (single feedback message covering whichever of quantitative/qualitative applies, per the spec's "combine into the exercise's overall result" + reusing Unit 06's one-feedback-bubble UI) — updated both call sites (`run-scoring.ts`, `wait-for-submission.ts`).
  - **Anomaly seeding**: `lib/tutor/generate-review-exercise.ts` — selects up to 4 real entries from `company_transaction_log` (Unit 09) and up to 3 anomaly-template-seeded distractors (half flagged anomalous, half as clean look-alikes, deterministic slice not random), calls the LLM (`lib/llm/prompts/review-exercise.ts`) to phrase them into natural review-packet text — **which items are anomalies is decided in code before the LLM call, never left to the LLM to judge**, then persists `ledger_review_items` as the server-only answer key. `lib/schemas/exercise.ts` gained `ReviewExerciseScenarioSchema` (`packet_items` instead of `transactions`) and `insertReviewExercise` (`lib/db/queries/exercises.ts`). **ASSUMPTION, documented in code**: this codebase has no "uncorrected past error" tracking on `company_transaction_log`, so real entries pulled from the log are always presented as clean (`is_anomaly: false`) — the spec's "some genuinely fine, some — if an uncorrected past error exists — genuinely anomalous" is honored as written, but no such case currently exists to find. Revisit if a later unit adds error tracking.
  - **Wait-for-submission job** (`lib/jobs/wait-for-submission.ts`, registered in `app/api/inngest/route.ts` alongside Unit 07's `runScoring`): triggered by a new `submission/part-received` event. Loops over `exercise.requiredParts`, re-reading `submission_parts` from the DB fresh before each `step.waitForEvent` call (so a part that arrived before the job even started is correctly detected, not waited for again — `waitForEvent` only catches events sent _after_ it runs), waiting up to `WAIT_WINDOW = '45m'` per missing part. **Concurrency correctness**: `submission/part-received` fires once per part arrival, but only the _first_ arrival for a submission should start a new function run — later arrivals must be seen by the already-running instance's `waitForEvent` matchers, not spawn a duplicate concurrent run. Solved via Inngest's `singleton: { key: 'event.data.submissionId', mode: 'skip' }` — while a run for a submission is active (including parked in `waitForEvent`), further triggers for the same `submissionId` are skipped as new runs but still reach the active run's matchers. Once the wait loop exits (complete, or window expired on the last missing part), proceeds to scoring with whatever arrived: runs the parse/validity-gate/scoring path if both file parts are present (reusing Units 05/06's `parseDayBookXml`/`parseTrialBalanceXml`/`runValidityGate`/`scoreSubmission` by import, same as Unit 07), runs `scoreQualitative` if a text part is present (grounded against `answer_key` for `explain_text`, against `ledger_review_items` for `review_text`), combines both via `combineOverallResult`, and threads any still-missing required parts into the coaching signal's `missingPartDescriptions` (plain-language, e.g. "your explanation never came through, so this was scored on the files alone") rather than silently omitting them or blocking forever. A gate failure on the file parts (genuine submission defect) still marks the submission `'invalid'`, same invariant as Units 05-07; a part missing at window-expiry does not block scoring, per the spec's explicit "never silently blocks forever."
  - **Simple two-file exercises stay on Unit 07's original job, unchanged**: `submitFiles` (`app/(chat)/chat/actions.ts`) checks `exercise.requiredParts.length === 2` and sends `submission/uploaded` (routing through `run-scoring.ts` exactly as before) for diagnostic/adaptive exercises; only exercises with more required parts (`'explain'`) send `submission/part-received` instead. Confirmed by code review: `run-scoring.ts` itself is otherwise unchanged except for `generateCoaching`'s call-site signature update and a null-guard on `daybook_path`/`trialbalance_path` (now nullable at the type level, but always populated on this job's path).
  - **Multi-part submission joining**: `getOpenSubmissionForExercise` (`lib/db/queries/submissions.ts`, new) finds a submission still in progress (`'validating'`/`'scoring'`) for the current exercise, so a second part arriving later (e.g. `explain_text` sent after the files, or the files uploaded after an explanation was already typed) joins the same `submissions` row instead of creating a new one — this is what makes out-of-order arrival work. `submitFiles` and the new `submitTextPart` (`app/(chat)/chat/actions.ts`) both check this before inserting. `ChatShell.tsx`'s `pendingSubmissionIds` list dedupes on `submission.id` so a text part and a file part that resolve to the same submission don't render `PendingSubmission` twice.
  - **Chat UI**: `Composer.tsx` rebuilt to show file-attach slots, a free-text input, or both depending on the active exercise's `requiredParts` (an `'explain'` exercise shows both — files first via their own "Send files" button, then the text input for the explanation; a `'review'` exercise shows only the text input) — no new input type, the existing composer text field just becomes live instead of permanently disabled, per the spec's "no special input UI, just the existing composer." `useSubmissionParts.ts` (new Realtime hook, same `postgres_changes` pattern as Unit 07's `useSubmissionStatus.ts`, INSERT events on `submission_parts` filtered to one submission) + `SubmissionPartsChecklist.tsx` (new presentational component — "Daybook ✓ · Trial Balance ✓ · Explanation — waiting", `status-success`/`text-muted` tokens exactly per the spec's design note) — only rendered by `PendingSubmission.tsx` when `requiredParts.length > 1`; a plain two-file exercise keeps Unit 07's single `ai-thinking` indicator, unchanged. `ChatShell.tsx`'s `exerciseToMessages` now renders `reviewPacketItems` when present (a review exercise has no `transactions`).
  - Dependencies: none new — built entirely on Units 04/06/07/09's existing LLM/Zod/Langfuse/Inngest/Realtime infrastructure, per spec.
  - New test files (colocated per code-standards rule 22): `lib/db/queries/submission-parts.test.ts` (completeness/missing-parts logic, including the review-exercise single-required-part case), `lib/tutor/score-qualitative.test.ts` (`combineOverallResult`'s worse-of-two-results logic in both directions, plus `groundingFromAnswerKey`). 11 new tests, 51/51 total passing. `lib/tutor/submission-gate.test.ts`'s fixture updated for `ExerciseForLearner`'s two new required fields (`reviewPacketItems`, `requiredParts`) — no behavior change, just kept the existing tests compiling against the extended type.
  - User confirmed live, full spec checklist:
    - [x] Apply `20260817120000_multipart_qualitative_scoring.sql` to the live Supabase project
    - [x] Seed `anomaly_templates` with real data
    - [x] A multi-part exercise correctly waits for all required parts before scoring, confirmed by sending them out of order across a real delay (not instantly)
    - [x] If the wait window expires with a part missing, scoring proceeds anyway and the feedback explicitly flags what wasn't received
    - [x] Simple two-file exercises (diagnostic/adaptive) are unaffected — confirmed no regression live
    - [x] Qualitative scoring produces sensible recall/precision/reasoning results against a constructed test case with known correct/incorrect flags
    - [x] Raw subscores are never shown to the learner as numbers — confirmed by reading actual generated coaching output
    - [x] `ledger_review_items` is never exposed to the client — confirmed by network tab inspection
    - [x] A review packet correctly mixes real entries with library-seeded distractors, confirmed by inspecting a generated packet
    - [x] RLS confirmed by direct test on all touched/new tables (`submission_parts`, `anomaly_templates`, `ledger_review_items`, plus the `submissions` nullable-column change)
    - [x] Status checklist UI updates live as parts arrive, confirmed in-browser
    - [x] `npm run build` passes with no TypeScript errors

- **Unit 1 — Project scaffold, design system** (spec: `context/specs/01-project-scaffold.md`): Next.js 16 (App Router, TS strict), Tailwind v4 in `@config` compat mode with `tailwind.config.ts` defining the full `ui-context.md` token set (neutral scale, light/dark semantic tokens as CSS variables, status colors, radius scale, Helvetica Neue `font-sans` / monospace `font-mono`), dark mode via `class` strategy with a FOUC-safe blocking init script + `ThemeToggle` client component (localStorage-persisted), `cn()` helper (`lib/cn.ts`), placeholder screen at `/` with token swatches. Folder structure per `architecture.md` Section 2 created (`/app/(auth)`, `/app/(chat)`, `/app/(progress)`, `/app/api`, `/lib/{llm,schemas,parsing,tutor,db,jobs}`, `/supabase/migrations`) as empty placeholders. Verified: `npm run build`, `npm run lint`, and TS strict all pass clean; dev server confirmed working in-browser by user, including a hydration-mismatch bug (OS dark-mode preference vs. server-rendered `<html>` class) found and fixed via `suppressHydrationWarning` on `<html>`. No CI/CD, auth, or DB code included — confirmed out of scope for this spec file (unlike `00-build.md`'s higher-level Unit 1 description, which mentions CI/CD).

- **Unit 2 — Auth** (spec: `context/specs/02-auth.md`, revised — see Architecture Decisions), code-complete for email + password, `npm run build` + `npm run lint` pass clean, core flow confirmed working in-browser by user:
  - `lib/supabase/client.ts` (browser client, `createBrowserClient`) and `lib/supabase/server.ts` (Server Component/Action client, `createServerClient` with cookie `getAll`/`setAll`) — the one addition to `/lib` this unit needed, per spec.
  - `proxy.ts` (Next.js 16's current convention, replacing the deprecated `middleware.ts`) — refreshes the Supabase session on every request and redirects unauthenticated requests to `/dashboard` (and any future `/dashboard/*`) to `/login`.
  - `app/(auth)/login/page.tsx` + `AuthForm.tsx` (client component) + `actions.ts` (Server Actions `logIn`, `signUp`, both `useActionState`-driven) + `auth-form-state.ts` (shared `AuthFormState` type/initial value — split out from `actions.ts` because a `'use server'` file may only export async functions, not plain constants) — centered card, `bg-surface-raised`, `radius-xl`, `border-default`; single email+password form toggling between log-in and sign-up mode. Sign-up branches on whether Supabase returns an active session (email confirmation off → immediate redirect to `/dashboard`) or not (confirmation on → "Check your email" state).
  - `app/auth/callback/route.ts` — Route Handler, exchanges the email-confirmation code via `exchangeCodeForSession`, redirects to `/dashboard` on success or back to `/login` with an error message on failure. Still needed even without OAuth, since confirmation links use the same PKCE code-exchange flow. Confirmed working end-to-end in a live test (sign-up → confirmation email → `/auth/callback` → `/dashboard`).
  - `app/dashboard/page.tsx` (Server Component, reads session server-side via `supabase.auth.getUser()`, redirects to `/login` if absent — belt-and-suspenders with the proxy check) + `actions.ts` (Server Action `logOut` calling `supabase.auth.signOut()` then redirecting to `/login`). Bare placeholder only, per spec: canvas background, "You're logged in" text, "Log out" button.
  - `.env.local` / `.env.example` — `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`; `.env.example` has placeholders only. `.gitignore` updated with `!.env.example` so it can be committed (the blanket `.env*` rule would otherwise have blocked it).
  - Dependencies added: `@supabase/supabase-js`, `@supabase/ssr`.

- **Unit 3 — Onboarding** (spec: `context/specs/03-onboarding.md`), `npm run build` + `npm run lint` pass clean, verified working end-to-end by user, including the RLS cross-learner denial test:
  - `supabase/migrations/20260812131725_learner_profile.sql` — new `learner_profile` table (`id` references `auth.users`, `license_mode` text check-constrained to `'licensed'|'educational'`, `books_begin_date` date not null, `onboarded_at` nullable timestamptz, `created_at`), RLS enabled with three policies (`select`/`insert`/`update`) scoped to `auth.uid() = id`. Applied manually by the user via the Supabase SQL editor/CLI (no `supabase/config.toml` in this repo, and the Supabase MCP connector is hard-wired to an unrelated project — see Session Notes).
  - `lib/schemas/onboarding.ts` — `LicenseMode` as an `as const` string union (not enum, per `code-standards.md` rule 6) and `OnboardingInputSchema` (Zod) validating `license_mode` + `books_begin_date`.
  - `lib/db/queries/learner-profile.ts` — `getLearnerProfile`, `isLearnerOnboarded`, `completeOnboarding` (upsert, sets `onboarded_at`). Single source for the onboarding-gate check, per spec (not duplicated inline).
  - `app/(auth)/onboarding/page.tsx` (Server Component) — redirects to `/login` if unauthenticated, to `/dashboard` if already onboarded (blocks re-submission), otherwise renders the form. Same card/token conventions as the Unit 2 login screen.
  - `app/(auth)/onboarding/OnboardingForm.tsx` (Client Component) — two-step single-screen flow: license mode as two selectable cards (Educational Mode reveals the inline plain-language date-restriction disclosure), Books Begin Date input defaulting to `2026-04-01`. "Continue" disabled until both fields have a value.
  - `app/(auth)/onboarding/actions.ts` + `onboarding-form-state.ts` — Server Action `submitOnboarding`: validates via `OnboardingInputSchema`, re-checks `auth.uid()` server-side, calls `completeOnboarding`, redirects to `/dashboard`. State/type split into its own file for the same `'use server'`-can-only-export-async-functions reason as Unit 2's `auth-form-state.ts`.
  - `app/dashboard/page.tsx` — now also calls `isLearnerOnboarded` and redirects to `/onboarding` if the learner hasn't completed it yet.
  - `proxy.ts` — `/onboarding` added to `PROTECTED_PATHS` alongside `/dashboard`, so unauthenticated visitors are redirected to `/login` before either Server Component gate runs.
  - Dependency added: `zod` (was absent from `package.json`; added per spec's "add it now if it genuinely isn't yet").
  - User confirmed live: onboarding gate redirect, form flow, and the RLS cross-learner read/write denial test all pass.

- **Unit 4 — Dashboard landing + chat shell + guided walkthrough + diagnostic exercise** (spec: `context/specs/04-dashboard-chat-diagnostic.md`), `npm run build` + `npm run lint` pass clean, fully verified working end-to-end by user against the spec's complete checklist (dashboard cards, walkthrough gating and one-time display, idempotent exercise generation, Zod validation/retry, answer-key exclusion from network payloads, Langfuse tracing, RLS, deterministic variant assignment):
  - `supabase/migrations/20260812140000_walkthrough_and_exercises.sql` — adds `walkthrough_completed_at timestamptz` to `learner_profile`; new `exercises` table (`id`, `learner_id` references `auth.users`, `kind` check-constrained to `'diagnostic'`, `scenario` jsonb, `answer_key` jsonb, `variant` check-constrained to `'A'|'B'`, `created_at`), RLS enabled with only a `select` policy scoped to `auth.uid() = learner_id` — no `insert`/`update` policy for `authenticated`, since all writes go through the service-role client (bypasses RLS by design, since answer_key must never be learner-writable). Applied to the live Supabase project by the user (same manual-apply pattern as Unit 3's migration).
  - `lib/supabase/service-role.ts` — new file, service-role Supabase client for trusted server contexts (Server Actions/jobs) that must bypass RLS. First use of this pattern; `architecture.md` Section 4 updated in the same change to explicitly cover Server Actions (previously only mentioned API routes/background jobs).
  - `lib/schemas/exercise.ts` — Zod schemas: `ExerciseScenarioSchema` (learner-facing: scenario, transactions, difficulty_level, variant) and `GeneratedExerciseSchema` (extends it with server-only `answer_key`). All types derived via `z.infer`.
  - `lib/llm/client.ts` — thin `fetch`-based OpenRouter client (chose this over an SDK — OpenRouter's API is OpenAI-compatible REST, so a wrapper is simpler than a dependency). Model pinned via `OPENROUTER_MODEL` env var, forces structured output via `response_format: json_schema`.
  - `lib/llm/tracing.ts` — Langfuse wrapper (`getTracedStructuredCompletion`) around the OpenRouter call; every call tagged with `traceName`, `userId` (learner ID), and `callType` metadata. `CallType` currently just `'diagnostic-generation'`, extend as more call types are added in later units.
  - `lib/llm/prompts/diagnostic-exercise.ts` — fixed diagnostic scenario prompt (L0, single-concept transactions) + JSON Schema for OpenRouter's structured output, plus a retry-prompt builder that feeds the prior validation error back in.
  - `lib/tutor/generate-exercise.ts` — `selectDiagnosticVariant` (deterministic: sha256 hash of learner ID, not random) and `generateDiagnosticExercise` (bounded 3-attempt retry loop: call LLM → Zod-validate → on failure, retry with validation error in prompt → on exhaustion, throw rather than persist unvalidated data).
  - `lib/db/queries/exercises.ts` — `insertExercise` (service-role write) and `getLatestDiagnosticExercise` (`ExerciseForLearner` return type has no `answer_key` field at the TypeScript level, and the Supabase `select()` never requests the column — belt-and-suspenders beyond RLS alone, per spec).
  - `lib/db/queries/learner-profile.ts` — added `walkthrough_completed_at` to `LearnerProfile`/`getLearnerProfile`, plus `hasCompletedWalkthrough` and `completeWalkthrough` (the update is guarded with `.is('walkthrough_completed_at', null)` so a second call is a no-op — this is the duplicate-generation guard for double-click/refresh on "I understand").
  - `app/dashboard/page.tsx` — replaced the Unit 2/3 placeholder with the two-card layout (Modules: muted/non-interactive, no route; Task: accent-bordered, links to `/chat`). Onboarding gate unchanged (reused from Unit 3).
  - `app/(chat)/chat/` — new route (`page.tsx` Server Component: auth + onboarding gate, fetches `learner_profile` and latest diagnostic exercise, passes to client shell). `ChatShell.tsx` (Client Component: steps through walkthrough, calls `confirmWalkthrough` Server Action on "I understand," swaps to exercise view). `message.ts` (minimal `ChatMessage` type: `role`, `content`, `kind` — deliberately not over-built, per spec). `walkthrough-config.ts` (static step content, conditionally includes the Educational Mode step based on `license_mode`, rendered client-side — not a DB table). `actions.ts` (`confirmWalkthrough` Server Action — idempotent via the `.is(...)` guard above, generates via service-role client, returns the client-safe `ExerciseForLearner`). `MessageBubble.tsx`, `ThinkingIndicator.tsx`, `Composer.tsx` — presentational, colocated per code-standards.md rule 21.
  - `proxy.ts` — `/chat` added to `PROTECTED_PATHS`.
  - `app/globals.css` / `tailwind.config.ts` — added the `ai-thinking` token (light/dark CSS variables + Tailwind color), the first unit to need it; wasn't in Unit 1's initial token set.
  - Dependency added: `langfuse`. No OpenRouter SDK — thin fetch wrapper instead (documented above, per spec's "agent's choice, document which").
  - `.env.local` / `.env.example` — added `OPENROUTER_API_KEY`, `OPENROUTER_MODEL` (set to `openai/gpt-4o-mini` per user's choice — confirmed structured-output support on OpenRouter), `LANGFUSE_SECRET_KEY`, `LANGFUSE_PUBLIC_KEY`, `LANGFUSE_BASE_URL`. User filled in real OpenRouter and Langfuse (self-hosted at `langfuse.prod.korefi.ai`) credentials in `.env.local` and confirmed the full spec checklist passes:
    - [x] Apply the new migration to the live Supabase project
    - [x] Fill in real `OPENROUTER_API_KEY` and Langfuse keys in `.env.local`
    - [x] Dashboard cards render correctly, Task navigates to `/chat`, in both light and dark mode
    - [x] Walkthrough shows Educational Mode step only for `license_mode === 'educational'`
    - [x] Walkthrough shows once only — revisiting `/chat` after completion skips straight to the exercise
    - [x] "I understand" does not create a duplicate `exercises` row on double-click or refresh
    - [x] A deliberately malformed LLM response triggers the retry path, not a crash
    - [x] Network tab check: `answer_key` never appears in any response payload for `/chat`
    - [x] Langfuse dashboard shows a trace for `diagnostic-generation` with learner ID and call type
    - [x] RLS cross-learner test on `exercises` (one learner cannot read another's row)
    - [x] Same learner regenerating (in testing) always gets the same variant

- **Unit 5 — Submission upload + XML parsing + validity gate** (spec: `context/specs/05-submission-upload-validity-gate.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 12/12 passing) all pass clean, fully verified working end-to-end by user: migration and Storage bucket/policies applied to the live Supabase project, cross-learner Storage RLS access confirmed blocked by direct test, and the full in-browser checklist confirmed (attach → chips → send → chip-only bubble → acknowledgment/corrective message → resubmission).
  - Real sample files `DayBook.xml` and `TrialBal.xml` (project root — found there after a scoped search; not previously listed in `context/`) are UTF-16LE with BOM, confirmed via `file`/hex inspection, and used as the actual fixtures for both unit tests and manual design verification. `TrialBal.xml` is the sparse 2-group-only sample the spec says must be rejected (`Current Liabilities`, `Purchase Accounts` — Tally group names, not ledger names; not a valid-TB fixture). `DayBook.xml` has exactly one `<VOUCHER VCHTYPE="Purchase">` dated `20260801`, `REPORTNAME: "All Masters"` (confirms the spec's structural-check-not-REPORTNAME-match note), 3 ledger entries confirming the sign convention (`Parekh Integrated Services Pvt Ltd`: `ISDEEMEDPOSITIVE=No` + `AMOUNT=444150.00` → Credit; `Material purchase`: `ISDEEMEDPOSITIVE=Yes` + `AMOUNT=-423000.00` → Debit), and empty `BILLALLOCATIONS.LIST` (parses as an empty string via fast-xml-parser, not an object — handled explicitly) on all three entries.
  - `supabase/migrations/20260812150000_submissions.sql` — new `submissions` table (`id`, `learner_id` references `auth.users`, `exercise_id` references `exercises`, `daybook_path`, `trialbalance_path`, `status` check-constrained to `'validating'|'valid'|'invalid'|'awaiting_scoring'`, `validity_errors` jsonb, `created_at`), RLS with `select`/`insert` policies scoped to `auth.uid() = learner_id`. Private `submissions` Storage bucket created with `select`/`insert` policies scoped via `(storage.foldername(name))[1] = auth.uid()::text`, enforcing the `submissions/{learner_id}/{submission_id}/...` path convention at the policy level. Applied to the live Supabase project by the user (same manual-apply pattern as prior migrations); cross-learner Storage access confirmed blocked by direct test.
  - `lib/schemas/voucher.ts` — Zod schemas for the normalized shape: `VoucherSchema` (`voucherType`, `date`, `ledgerEntries: [{ledgerName, amount, drOrCr, billAllocations}]`), `ParsedDayBookSchema`, `ParsedTrialBalanceSchema`. All parser output types derived via `z.infer`, per code-standards rule 4.
  - `lib/parsing/daybook.ts` / `lib/parsing/trialbalance.ts` — `fast-xml-parser`-based parsers. UTF-16LE-with-BOM detected by leading `FF FE` bytes and decoded explicitly (`buffer.toString('utf16le', 2)`), not assumed; UTF-16BE (`FE FF`) explicitly rejected rather than silently mis-parsed. Daybook parser keys off structural content (`ENVELOPE > BODY > IMPORTDATA > REQUESTDATA > TALLYMESSAGE[] > VOUCHER[]`, `LEDGERENTRIES.LIST[]`) rather than `REPORTNAME`, per the spec's explicit instruction — flagged in code as provisional (`// ASSUMPTION`) until verified against a real Day Book (not "All Masters") export. Trial Balance parser reads paired sibling `DSPACCNAME[]`/`DSPACCINFO[]` arrays under `ENVELOPE` (confirmed structure — Tally's TB export has no tag distinguishing group-level from ledger-level rows). Both throw a typed `*ParseError` with a specific message on malformed XML or missing structural elements, never a generic failure or crash.
  - `lib/tutor/submission-gate.ts` — `runValidityGate`, checks (2)-(4) from the spec (file-parse-error check (1) happens at the call site, since a parse failure throws before the gate runs): TB ledger-row-count-too-sparse (see below), voucher dates within `[books_begin_date, today]`, voucher count vs. `exercise.transactions.length`. Answer key is never touched. Each failure produces a specific `{code, message}`, never a generic "invalid file" — messages are plain-language and corrective per the spec's "friendly" tone direction.
  - **Flagged assumption**: the TB export format (confirmed against the real sample) has no tag distinguishing a group-level row from a ledger-level row — both are just a `DSPACCNAME`/`DSPACCINFO` pair. With no better structural signal available, "too sparse" is implemented as a minimum row count (`MIN_TRIAL_BALANCE_LEDGER_ROWS = 3`, chosen only to exceed the real sample's 2 rows) rather than a content-aware check. Documented in code with a comment; revisit once a real ledger-level TB sample is available to calibrate against.
  - `lib/db/queries/submissions.ts` — `insertSubmission`, `updateSubmissionStatus`.
  - `app/(chat)/chat/actions.ts` — added `submitFiles` Server Action (same file as `confirmWalkthrough`, per code-standards rule 16 — one `actions.ts` per route segment): authenticates, validates both are `.xml`, uploads to the learner-scoped Storage path, inserts the `submissions` row (`status: 'validating'`), parses both files, runs the validity gate, and updates status to `'invalid'` (with `validity_errors`) or `'awaiting_scoring'`. Synchronous within the action, no background job — per spec, this is intentionally replaced (not layered on top of) when Unit 07 adds job durability.
  - `app/(chat)/chat/Composer.tsx` — rebuilt from Unit 4's disabled text-only placeholder into two explicit labeled attach slots (Day Book XML, Trial Balance XML — asked the user how to disambiguate the two files since the spec didn't specify; chose explicit labeled slots over filename-sniffing). Client-side `.xml` extension check per slot before allowing send; attached files render as chips (`radius-sm`, `bg-surface`, remove button) above the composer before sending. Send disabled until both slots are filled.
  - `app/(chat)/chat/message.ts` — `MessageKind` extended with `'submission'`, `'submission-result-valid'`, `'submission-result-invalid'`; `ChatMessage` gained optional `attachmentNames` (filenames only — the raw XML never enters this type or gets rendered).
  - `app/(chat)/chat/MessageBubble.tsx` — renders `attachmentNames` as chips (never raw XML content, per spec); `'submission-result-invalid'` messages get a small inline `status-error`-token flag ("Needs a fix") rather than a full-bleed red panel, per the spec's "corrective, not alarming" direction.
  - `app/(chat)/chat/ChatShell.tsx` — `handleSubmitFiles` appends a learner message (attachment chips only) immediately, then calls `submitFiles`; on success appends either the acknowledgment message (valid, `awaiting_scoring` — explicitly no score shown) or the corrective message (invalid, one bullet per specific reason). A new upload creates a new `submissions` row tied to the same `exercise_id`, satisfying the spec's resubmission requirement without any special-case code.
  - Dependencies added: `fast-xml-parser`. Also added `vitest` (dev) — first unit to need automated tests for non-UI logic (parsing/gate), per `architecture.md`'s stack table and `ai-workflow-rules.md` Section 7's requirement that parsing/scoring/mastery logic have tests, not just build passing. `vitest.config.ts` added (path alias `@/*` matching `tsconfig.json`), `package.json` gained a `test` script.
  - New test files (colocated per code-standards rule 22): `lib/parsing/daybook.test.ts`, `lib/parsing/trialbalance.test.ts`, `lib/tutor/submission-gate.test.ts` — all run against the real sample XML files, not synthetic fixtures alone, confirming the sign convention and sparse-TB rejection against actual data.
  - User confirmed live:
    - [x] Apply `20260812150000_submissions.sql` to the live Supabase project (table, RLS policies, `submissions` Storage bucket + policies)
    - [x] RLS/Storage cross-learner access test, done directly (not assumed)
    - [x] In-browser verification of the full spec checklist (attach → chips → send → chip-only bubble → acknowledgment or corrective message → resubmission)

- **Unit 8 — Hint ladder** (spec: `context/specs/08-hint-ladder.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 24/24 passing, unchanged — this unit's own logic (`hint-ladder.ts`'s rung selection) is a one-line count+cap with no branching worth a dedicated test, matching how Unit 4/6's LLM-generation code wasn't unit-tested either, only exercised live) all pass clean. Fully verified working end-to-end by user against the spec's complete checklist: migration applied to the live Supabase project, RLS confirmed by direct test, and the full live click-through (all 5 rungs, rung-6 repeat behavior, cross-exercise independence, network payload check, flat rung styling, Langfuse trace) all confirmed.
  - `supabase/migrations/20260813130000_hint_requests.sql` — new `hint_requests` table (`id`, `exercise_id` references `exercises`, `learner_id` references `auth.users`, `rung` smallint check-constrained `1–5`, `hint_content` jsonb, `created_at`), RLS with `select`/`insert` policies scoped to `auth.uid() = learner_id` — unlike `exercises`/`scoring_results`, learners _can_ insert their own hint request rows directly (the row itself is safe for them to write; only the LLM-generated content within it needs server-side grounding). Keyed off `exercise_id` + `learner_id`, not `submission_id`, per the spec's explicit UX note (a learner might ask for a hint while still working in Tally, before ever uploading). Applied to the live Supabase project by the user; RLS confirmed by direct test.
  - `lib/schemas/hint.ts` — `HintSchema` (Zod): `rung` (1–5 literal union), `hint_text`, `concept_tag`. Type derived via `z.infer`, per code-standards rule 4.
  - `lib/tutor/hint-ladder.ts` — `determineNextRung`: rung = (count of prior `hint_requests` for this exercise) + 1, capped at 5 via `Math.min`. Once rung 5 has been given, every further call keeps returning 5 — never errors, never repeats an earlier rung, never dead-ends. Pure function over a DB count, no LLM call.
  - `lib/db/queries/hint-requests.ts` — `countHintRequestsForExercise` (the only input the rung logic needs), `insertHintRequest`, `getHintDepthForExercise` (alias over the count query — the queryable surface Unit 09 will read later; this unit doesn't consume it).
  - `lib/llm/prompts/hint.ts` — `buildHintPrompt`/`buildHintRetryPrompt`, with `RUNG_INSTRUCTIONS` giving each of the 5 rungs a distinct disclosure-level instruction (rung 1 direction-only, up through rung 5 explicitly instructed to be the genuine full worked answer with a check-for-understanding question, not a stronger rung 4). The exercise's `answer_key` entries are serialized into the prompt as LLM grounding context only — explicitly instructed never to be quoted verbatim in the response, and the response schema (`hint.ts`) has no field capable of carrying the raw answer key structure back out.
  - `lib/tutor/generate-hint.ts` — `generateHint`: bounded 3-attempt retry loop (same pattern as `generate-exercise.ts`/`generate-coaching.ts`), Zod-validates against `HintSchema`, throws rather than persisting/returning unvalidated output.
  - `lib/llm/tracing.ts` — `CallType` extended with `'hint-generation'`. Added an optional `extraMetadata` field to `TracedCompletionParams` (merged into both the trace's and generation's `metadata` alongside `callType`) — the spec requires the hint trace to be tagged with the rung number specifically, and no prior call type needed per-call metadata beyond `callType`, so this was the minimal extension rather than a rung-specific one-off. Confirmed live on the Langfuse dashboard: trace exists for hint-generation calls, tagged with rung number.
  - `app/(chat)/chat/actions.ts` — added `requestHint` Server Action: re-derives the next rung server-side (never trusts a client-supplied rung), fetches the answer key via the service-role client (same pattern as the scoring pipeline), calls `generateHint`, then inserts the `hint_requests` row via the **authenticated** client (not service-role) so the row actually exercises the table's own RLS insert policy. Returns only the composed `Hint` — `answer_key` never leaves the function. Confirmed via network tab: `answer_key`'s raw structure never appears in the response payload, even on rung 5.
  - `app/(chat)/chat/message.ts` — `MessageKind` extended with `'hint'`; `ChatMessage` gained an optional `hint: Hint` field, documented as rendering only the composed hint fields, never anything answer-key-derived.
  - `app/(chat)/chat/MessageBubble.tsx` — renders hint messages with the `ai-hint-rung` token look from `ui-context.md` (`bg-accent-subtle`/`text-accent`, deliberately identical styling at every rung — no escalating color ramp) as a small "Hint N of 5" label above the hint prose. Visually confirmed: rung 1 and rung 5 bubbles look the same, no alarming red-ramp.
  - `app/(chat)/chat/Composer.tsx` — new hint button below the send row, shown whenever the composer isn't `disabled` (i.e. an exercise is active — same gating Composer already had for walkthrough/pre-exercise states, reused rather than duplicated). Label reads "I'm stuck — get a hint" before any hint has been requested, "Still stuck? Get another hint" after, per the spec's explicit wording requirement — confirmed correct in-browser.
  - `app/(chat)/chat/ChatShell.tsx` — `handleRequestHint` calls the Server Action and appends the hint message to the same `submissionMessages` timeline array used for submission results (chronological single-timeline append, not a separate hint-only list, so hint and submission messages interleave in the order they actually happened). `hasRequestedHint` state initialized from a new `initialHintDepth` prop (not just session-local), so the button label is correct even on a fresh page load/reload, not only after a hint requested in the current browser session.
  - `app/(chat)/chat/page.tsx` — fetches `initialHintDepth` via `getHintDepthForExercise` alongside the existing `initialExercise` fetch, passed down to `ChatShell`.
  - Dependencies: none new — built entirely on the LLM/Zod/Langfuse plumbing already established in Unit 4, per spec.
  - User confirmed live, full spec checklist:
    - [x] Requesting a hint for the first time on an exercise returns rung 1 content
    - [x] Requesting again returns rung 2, then 3, then 4, then 5 — confirmed by actually clicking through five times
    - [x] Requesting a sixth time returns rung 5 again (the full answer), not an error or a repeat of an earlier rung
    - [x] Rung 5's content is genuinely the full worked answer, not a stronger version of rung 4
    - [x] Rung 2's video reference uses a placeholder concept-tag pointer, doesn't break because no real video library exists yet
    - [x] Hints for one exercise don't leak into or affect hint rung state for a different exercise — tested by requesting hints on two different exercises
    - [x] The hint button's label correctly reflects "get the next hint" once at least one has been given, not a generic repeatable label
    - [x] All rung bubbles use the flat, non-escalating `ai-hint-rung` styling — rung 1 and rung 5 don't look alarmingly different
    - [x] `answer_key`'s raw structure is never returned to the client — checked the actual network payload, even on rung 5
    - [x] `hint_requests` RLS confirmed by direct test
    - [x] Langfuse trace exists for hint generation calls, tagged with rung number
    - [x] `npm run build` passes with no TypeScript errors
    - [x] No mastery-engine logic reads or reacts to hint depth yet — confirmed by code review; `getHintDepthForExercise` is defined but not called from anywhere except `page.tsx`'s initial-render fetch for the button label, which is UI state, not mastery/scoring logic

- **Unit 7 — Background jobs + durable scoring** (spec: `context/specs/07-background-jobs-durable-scoring.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 24/24 passing, unchanged from Unit 6 — this unit added no new pure-logic to test, only job/transport plumbing) all pass clean. Fully verified working end-to-end by user: migration applied to the live Supabase project, a real submission triggered a full gate → score → coach run, the mid-job kill/restart durability test passed, chat UI status updates confirmed live via Realtime with no manual refresh, and the cross-learner Realtime RLS test confirmed blocked by direct test.
  - `lib/jobs/client.ts` — the Inngest client (`id: 'ai-tutor'`). `app/api/inngest/route.ts` — the serve Route Handler (`serve({ client, functions: [runScoring] })`), per Inngest's Next.js App Router integration pattern.
  - `lib/jobs/run-scoring.ts` — the one Inngest function (`run-scoring`, triggered by `submission/uploaded`), steps in order, each wrapped in `step.run` so a failed step retries without re-running completed ones: (1) fetch the submission + its exercise + the learner profile, (2) download both XML files from Storage and parse them (Unit 5's `parseDayBookXml`/`parseTrialBalanceXml`, imported as-is), (3) run the validity gate (Unit 5's `runValidityGate`, imported as-is) — on failure, mark `status: 'invalid'` with `validity_errors` and stop, (4) on success, mark `status: 'scoring'`, then score (Unit 6's `scoreSubmission`, imported as-is, no LLM call), (5) generate coaching (Unit 6's `generateCoaching`, imported as-is), (6) persist the `scoring_results` row and mark `status: 'scored'`. No business logic is reimplemented in this file — confirmed by inspection that parsing, the gate, scoring, and coaching each have exactly one implementation in the codebase (Units 05/06's files), only imported here.
  - **Idempotency**: added a `unique (submission_id)` constraint on `scoring_results` (new migration) and changed `insertScoringResult` (`lib/db/queries/scoring-results.ts`, Unit 6's file) from `insert` to `upsert(..., { onConflict: 'submission_id' })`, so a step retry after a partial failure (e.g. the row insert succeeds but the function crashes before the status update) can't create a duplicate row — the retried step just re-applies the same result.
  - **Refactored `submitFiles`** (`app/(chat)/chat/actions.ts`, Unit 5's file): now uploads both files to Storage, creates the `submissions` row (`status: 'validating'`), sends the `submission/uploaded` event via `inngest.send()`, and returns immediately (`{status: 'accepted', submission}`) — no longer awaits parsing, the gate, scoring, or coaching. Verified by design (the event send + return is synchronous DB/Storage work only, no LLM/job wait) that the response comes back well before scoring would realistically finish; **not yet timed against a live run by the user**. Added `getScoringFeedback` Server Action (same file) — called by the client only after its Realtime subscription observes `status: 'scored'`, fetches via Unit 6's `getFeedbackForLearner` (already excludes `error_codes` at the query level).
  - **`submissions.status` narrowed** (`lib/db/queries/submissions.ts`, Unit 5's file) from `'validating' | 'valid' | 'invalid' | 'awaiting_scoring'` to `'validating' | 'invalid' | 'scoring' | 'scored'` per spec — `'valid'` was dead (no code path ever wrote it) and `'awaiting_scoring'` is superseded by the `'scoring'`/`'scored'` pair so the client can tell "gate passed, not started" from "scoring in progress" (both render the same `ai-thinking` indicator today, but the job needs the finer states to update status once per step). Added `getSubmission` (fetch-by-id) alongside the existing `insertSubmission`/`updateSubmissionStatus`.
  - Added `getExerciseById` (`lib/db/queries/exercises.ts`, Unit 4's file) — the job has a submission's `exercise_id`, not a learner's "latest" exercise, and a resubmission must always score against the exercise it was originally created against, not whatever is currently latest. Same `answer_key`-exclusion pattern as `getLatestDiagnosticExercise`.
  - `supabase/migrations/20260813120000_submission_status_and_realtime.sql` — drops/re-adds `submissions`'s status check constraint for the new set; adds the `scoring_results.submission_id` unique constraint above; adds `submissions` and `scoring_results` to the `supabase_realtime` publication (`alter publication supabase_realtime add table ...`) — required for Postgres Changes events to fire at all; existing RLS `select` policies on both tables are enforced per-subscriber automatically once a table is published, confirmed via Supabase's Realtime docs (no separate realtime-specific policy needed). Applied to the live Supabase project by the user.
  - **Client-side live updates**: `app/(chat)/chat/useSubmissionStatus.ts` (hook: subscribes to `postgres_changes` UPDATE on `submissions` filtered to `id=eq.<submissionId>` via the browser Supabase client, so the subscription carries the learner's session/RLS — cleans up with `removeChannel` on unmount). `app/(chat)/chat/PendingSubmission.tsx` (one instance per in-flight submission id: renders `ThinkingIndicator` — reusing Unit 4's component, not a new one — while status is `'validating'`/`'scoring'`; on `'invalid'` builds the corrective message directly from the row's `validity_errors` payload; on `'scored'` calls `getScoringFeedback` and reports the result upward via `onResult`). `ChatShell.tsx` — `handleSubmitFiles` now just appends the learner's chip bubble and adds the new submission id to a `pendingSubmissionIds` list; one `PendingSubmission` renders per pending id; `handleSubmissionResult` appends the final message and clears that id from the pending list. No polling anywhere in this path. Message rendering itself (`MessageBubble.tsx`, `message.ts`'s `MessageKind` values) is unchanged from Units 05–06, per spec — only the delivery mechanism changed.
  - `.env.local` / `.env.example` — added `INNGEST_DEV=1` (forces local dev mode against the Inngest Dev Server; without it the SDK assumes production/cloud mode and 500s), `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY` (blank locally, placeholders in `.env.example`, required in production).
  - Dependency added: `inngest` (`^4.18.0`). No new Supabase package — Realtime is part of the already-installed `@supabase/supabase-js`, per spec.
  - User confirmed live, full spec checklist:
    - [x] The upload Server Action returns immediately
    - [x] The Inngest function runs the full gate → score → coach pipeline end to end, reusing Units 05/06's logic via import only — exercised by an actual triggered run through the chat UI, not just app/function discovery
    - [x] Killing the dev server mid-job and restarting it resumes/completes the job correctly
    - [x] Chat UI shows `ai-thinking` while `status` is `'validating'`/`'scoring'` and updates live on `'invalid'`/`'scored'`, no manual refresh
    - [x] Realtime updates confirmed learner-scoped by direct test
    - [x] No duplicate `scoring_results` row on step retry
    - [x] Units 05/06's logic reused via import, not duplicated — confirmed: exactly one implementation each of parsing, the gate, scoring, and coaching in the codebase, all imported into `run-scoring.ts`
    - [x] `npm run build` passes with no TypeScript errors
    - [x] No multi-part/out-of-order submission handling exists yet (Unit 11) — confirmed by code review, `run-scoring.ts` only handles the single daybook+TB pair pipeline

- **Unit 6 — Scoring engine + Answer Key comparison + Coaching feedback** (spec: `context/specs/06-scoring-engine-feedback.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 20/20 passing at the time, later 24/24 once Unit 6's own bug-fix regression tests landed — see below) all pass clean. Fully verified working end-to-end by user: migration applied to the live Supabase project, RLS confirmed by direct test, in-browser feedback check confirmed, and Langfuse trace confirmed for the coaching call.
  - **Cross-boundary schema change, flagged before making it**: the spec's diff list (GST head+rate, TDS section+rate+base, voucher type, bill-by-bill reference, narration) didn't match the Unit 4 answer key shape (`sequence, correct_account, dr_cr, amount` only) or the Unit 5 parsed voucher shape (no structured GST/TDS fields). Asked the user how to resolve both gaps before writing scoring logic, per `ai-workflow-rules.md` Section 4. Resolutions (both user-confirmed):
    1. Extended `AnswerKeyEntrySchema` (`lib/schemas/exercise.ts`, Unit 4's file) with `voucher_type`, `gst_head`, `gst_rate`, `tds_section`, `tds_rate`, `tds_base`, `bill_reference`, `narration` (all nullable except `voucher_type` — nullable means "this transaction genuinely has none," not "not yet provided," so the LLM must state null explicitly, not omit). Updated the diagnostic exercise's JSON Schema and system prompt (`lib/llm/prompts/diagnostic-exercise.ts`) to populate them, with instruction that L0 diagnostic transactions have no GST/TDS component (nulled) unless explicitly a bill-settling entry.
    2. Since the Unit 5 parser only extracts raw ledger name + amount (no structured tax fields), GST/TDS signal on the _parsed submission_ side is inferred in `score-submission.ts` itself by matching ledger name text against known patterns (`/\bIGST\b/i` etc., `/\bTDS\b/i`) — Unit 5's parser stays untouched; the accounting-domain interpretation lives in the scoring file per the spec's "deterministic in code" boundary. Documented as `// ASSUMPTION` in code.
  - **Rulebook grounding gap, also flagged before proceeding**: the spec says `generate-coaching.ts` takes "the Rulebook grounding context" (the Karbon VA House Practices Rulebook, `project-overview.md` goal 6) — no such file/table/ingestion exists anywhere in the codebase yet. User confirmed: use a static placeholder grounding block (general Indian-accounting best-practice text — GST head selection, TDS applicability, bill-by-bill discipline, narration standards) hard-coded in `lib/llm/prompts/coaching.ts`, clearly commented as standing in for the real Rulebook until it's ingested (consistent with the project's lazy-plumbing principle already applied to LLM/Zod/Langfuse/PDF/job-runtime in other units). Swappable later without restructuring the prompt shape.
  - `lib/schemas/scoring.ts` — `ScoringResultSchema` (`per_voucher_diffs`, `tb_tie_out`, `weighted_score`, `overall_result`), `SCORING_ERROR_CODES` (17 internal codes), `SCORED_FIELDS` (8 fields, each with a fixed weight). `VoucherDiff.expected_masked` is `z.literal(true)` — the schema shape itself makes it impossible to carry an actual expected value.
  - `lib/tutor/score-submission.ts` — pure function, **no LLM call, no network I/O** (confirmed by inspection, not just behavior — grepped for any LLM/network reference, none found). Diffs each parsed Day Book voucher (matched to the answer key by `sequence`, in order) against the answer key entry-by-entry: account (ledger-name match), Dr/Cr, amount (tolerance `0.005` for float rounding), voucher type, GST (inferred from ledger name), TDS (inferred from ledger name), bill reference (from `billAllocations`), narration (always reports missing — Unit 5's parser doesn't extract a `NARRATION` field from the XML at all, confirmed by inspecting the real sample's raw XML, which does have a `<NARRATION>` tag the parser ignores; documented as `// ASSUMPTION`, revisit when the parser is extended). Trial Balance tie-out nets the answer key's Dr/Cr entries per account and compares against the parsed TB's closing balances. `PASS_THRESHOLD = 0.9`, `PARTIAL_THRESHOLD = 0.6` as named constants (not magic numbers); GST/TDS fields carry `GST_TDS_WEIGHT = 2` vs. `STANDARD_WEIGHT = 1` for every other field.
  - `lib/schemas/coaching.ts` — `CoachingSchema` (`result_line`, `praise`, `flagged_areas: string[]`, `next_step_note`), no error codes or weights on this schema by construction.
  - `lib/llm/prompts/coaching.ts` — system prompt explicitly instructs the model never to state the literal correct answer (e.g. "never say you should have used IGST not CGST/SGST") and to keep `flagged_areas` concept-level; includes the Rulebook placeholder above. `lib/tutor/generate-coaching.ts` — `buildCoachingSignal` converts scoring diffs into concept-level prose descriptions only (e.g. "the GST treatment on transaction 1") before they ever reach the LLM prompt — the internal error code and any expected value never leave `score-submission.ts`'s output. Bounded 3-attempt retry on Zod validation failure (same pattern as Unit 4's `generate-exercise.ts`). `lib/llm/tracing.ts`'s `CallType` extended with `'coaching'`.
  - `supabase/migrations/20260812160000_scoring_results.sql` — new `scoring_results` table (`id`, `submission_id` references `submissions`, `exercise_id` references `exercises`, `learner_id` references `auth.users`, `weighted_score`, `tb_tie_out`, `error_codes` jsonb, `overall_result`, `feedback_text` jsonb, `created_at`), RLS with only a `select` policy scoped to `auth.uid() = learner_id` — no `insert`/`update` policy for `authenticated`, since scoring is computed server-side (same pattern as Unit 4's `exercises` table). Applied to the live Supabase project by the user; RLS confirmed by direct test.
  - `lib/db/queries/scoring-results.ts` — `insertScoringResult` (service-role write) and `getFeedbackForLearner` (explicitly selects only `overall_result, feedback_text` — never `error_codes` — regardless of what RLS would allow, same belt-and-suspenders pattern as `getLatestDiagnosticExercise` excluding `answer_key`). Not yet wired into any read path in this unit (no route re-fetches historical feedback yet — the chat flow renders it directly from the Server Action's return value); added per spec for when a later unit needs it.
  - `lib/db/queries/exercises.ts` — added `getExerciseAnswerKey` (server-only, fetches `answer_key` by exercise ID for scoring — must only ever be called from server-side scoring code, never from a code path shaping a client response).
  - `app/(chat)/chat/actions.ts` — `submitFiles` extended: once the validity gate passes and status is set to `awaiting_scoring`, immediately (still synchronous, no job infra — Unit 7's job) calls `getExerciseAnswerKey` → `scoreSubmission` → `generateCoaching` → `insertScoringResult`, then returns a new `'scored'` result variant carrying only `overallResult` and `feedback` (the coaching schema's output) — `scoringResult`/`errorCodes` never leave the function into the returned payload. Resubmission (a new `submissions` row against the same `exercise_id`, per Unit 5) triggers this same path with no special-casing, since the whole flow keys off the exercise's immutable answer key.
  - Chat UI (`app/(chat)/chat/message.ts`, `MessageBubble.tsx`, `ChatShell.tsx`): `MessageKind`'s `'submission-result-valid'` replaced with `'submission-result-scored'`, carrying a `scoringFeedback: { overallResult, feedback }` field. `MessageBubble` renders, in spec order: a result badge (`status-success`/`status-warning`/`status-error` tint + text label "Pass"/"Partial"/"Needs work" — color never carries the meaning alone), bold `result_line`, `praise` paragraph, `flagged_areas` as a bulleted list (omitted if empty), `next_step_note`. Only the composed `feedback_text` fields render — no raw error codes or internal values ever reach this component, by construction (the type carrying them was never sent to the client).
  - Dependencies: none new — built entirely on Units 4/5's LLM client, Zod, Langfuse, and parsed voucher data, per spec.
  - `lib/tutor/score-submission.test.ts` — new test file (Vitest), built against the real `DayBook.xml` sample (hand-inspected: one Purchase voucher, `Material purchase` Dr 423000, `IGST Payable` Dr 21150 [5% of 423000], `Parekh Integrated Services Pvt Ltd` Cr 444150 — confirms the sample does carry a GST line, useful as a worked example). Hand-verified: a fully-correct answer key scores `weighted_score = 0.9` (narration always misses, weight 1 of total weight 10) and `overall_result = 'pass'` (meets `PASS_THRESHOLD` with TB tie-out true); a GST-head mismatch drops the score to `0.7` (narration weight 1 + GST weight 2 wrong, out of 10) — confirms GST errors weight strictly more than a narration-only miss, same test file covers `DR_CR_REVERSED`, `ACCOUNT_WRONG`, `VOUCHER_MISSING`, TB tie-out failure, and `collectErrorCodes`. 8 new tests, 20/20 total passing.
  - User confirmed live, full spec checklist:
    - [x] `score-submission.ts` tested against a real worked example (hand-verified diffs/tie-out/weighted score, not just "it ran")
    - [x] GST/TDS 2x weighting confirmed via a comparative test case
    - [x] `score-submission.ts` makes no LLM call (confirmed by grep/inspection)
    - [x] Coaching feedback generated via LLM, Zod-validated, bounded retry
    - [x] Feedback text flags concept-level areas only, verified by reading actual generated output against a known-wrong submission
    - [x] `answer_key`/`error_codes` never appear in any client-facing payload — confirmed by code inspection (only `overall_result`/`feedback` leave the Server Action)
    - [x] Result badge renders with correct token-based color, matches `overall_result`
    - [x] `scoring_results` RLS confirmed by direct test
    - [x] Langfuse trace exists for the coaching call, tagged `call_type: 'coaching'`
    - [x] Re-submission after rejection correctly triggers scoring on the corrected submission
    - [x] `npm run build` passes with no TypeScript errors
    - [x] No hint ladder, mastery tracking, or history-aware feedback exists in this unit (single-submission-scoped only, confirmed by code review — nothing reads prior `scoring_results` rows)
  - **Real bug found and fixed during manual testing (2026-08-13)**: user attempted to test by uploading the Unit 5 fixture XMLs (`xmls/DayBook.xml`/`TrialBal.xml`, single Purchase voucher) against a live diagnostic exercise ("Alpha Traders," 4 transactions) — correctly rejected by the validity gate (voucher count + TB sparseness mismatch, gate working as intended, not a bug). Investigating led to fetching the exercise's actual `answer_key` from the DB directly (service-role script, output never shown to the browser), which revealed `score-submission.ts` had a real correctness bug: the diff logic assumed **one answer-key entry per voucher**, matched by array index. Real generated answer keys are full double-entry — **one entry per ledger leg** (a Dr leg + a Cr leg sharing the same `sequence`), confirmed against the live exercise's actual 8-entries-for-4-transactions shape. This caused two failures: (1) index-based matching went out of range past the first couple of transactions once there were 2 legs per sequence, wrongly flagging later transactions `VOUCHER_MISSING`; (2) even for a single transaction, only the _first_ ledger entry matching _one_ expected leg was ever checked — a learner could nail one leg and post total garbage on the other leg of the same voucher and still score full marks on `account`/`dr_cr`/`amount`, since the second leg was never diffed at all.
    - **Fix**: `score-submission.ts` now groups `answerKey.entries` by `sequence` (`groupAnswerKeyEntriesBySequence`) before mapping to vouchers, and `diffVoucherAgainstAnswerKey` takes the full array of expected legs for a transaction, matching each leg against an unmatched ledger entry on the voucher (removing matched entries from the pool so two legs can't both match the same ledger line). Voucher-level fields (`voucher_type`/`gst`/`tds`/`bill_reference`/`narration`) are still diffed once per voucher (against the first leg), not once per leg, since they're transaction-level facts, not per-leg facts.
    - 4 new regression tests added to `score-submission.test.ts` under a `describe('double-entry answer keys...')` block, including one named specifically for the original bug ("does not let one correct leg mask the other leg being wrong") and one confirming a second transaction maps to the second voucher without an out-of-range index. 24/24 tests passing (was 20).
    - **Separately discovered while testing**: `DayBook.xml`/`TrialBal.xml` had been moved by the user from the project root into a new `xmls/` folder (through the IDE/UI, not by any unit's code), which broke all 4 existing test files that hardcoded the old root-relative path. Fixed by updating the `path.resolve` calls in `daybook.test.ts`, `trialbalance.test.ts`, `submission-gate.test.ts`, and `score-submission.test.ts` to `../../xmls/...` — unrelated to the scoring bug, but blocked running any tests until fixed. User confirmed this fix (not moving the files back).
    - **Stale data cleanup**: the user's existing exercise row (generated before this unit's schema change) had an `answer_key` in the old pre-Unit-6 shape (no `voucher_type`/`gst_head`/etc. fields at all). User confirmed deleting that exercise row (service-role script, output not shown to browser) and resetting `learner_profile.walkthrough_completed_at` to null so a fresh exercise — matching the current schema — generates on the next `/chat` visit. Any `submissions`/`scoring_results` rows tied to that exercise cascaded away with it (`ON DELETE CASCADE`, as designed).
    - Scratch scripts used for DB inspection/cleanup were written to the OS temp scratchpad and to a git-ignorable `_scratch-*.mjs` in the project root (to resolve `node_modules`), both deleted after use — not committed, not part of the codebase.

- **Unit 9 — Mastery engine + adaptive exercise generation** (spec: `context/specs/09-mastery-engine-adaptive-generation.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 56/56 passing, up from 40 — 16 new tests: `mastery.test.ts` new, plus 4 new `concept_results` cases in `score-submission.test.ts`) all pass clean. Fully verified working end-to-end by user against the spec's complete checklist: migration applied to the live Supabase project, RLS confirmed by direct test on all four new tables, and the full live click-through (adaptive generation after the diagnostic, mastery streak, hint-rung-heavy pass exclusion, reinforcement level-drop, escalation, company-registry continuity, auto-delivery) confirmed.
  - **Two ambiguities flagged and resolved with the user before writing code**, per `ai-workflow-rules.md` Section 4:
    1. Nothing in the codebase produced a `concept_tag` (the answer key and scoring pipeline only had generic field names like `gst`/`tds`, not accounting concepts). User confirmed: the LLM assigns `concept_tags` per answer-key entry from a new fixed vocabulary (`CONCEPT_TAGS` in `lib/schemas/exercise.ts` — 11 tags spanning the 6 voucher types plus gst/tds/bill-reference/narration/TB-tie-out), not a reuse of `scoring.ts`'s `SCORED_FIELDS`.
    2. No prior spec defined a numbered "module" for the UI's "Module 3 · Level 2" label. User confirmed: `module_number` is derived, not stored — count of concepts at `status: 'mastered'` plus 1 (`getModuleNumber` in `lib/db/queries/mastery.ts`). "Level" maps directly to the existing `difficulty_level` (L0-L4).
  - **Cross-boundary schema changes, flagged before making them** (same pattern as Unit 6's answer-key extension):
    - `lib/schemas/exercise.ts` (Unit 4's file): added `CONCEPT_TAGS`/`ConceptTag`, and `AnswerKeyEntrySchema` gained `concept_tags: z.array(z.enum(CONCEPT_TAGS)).min(1)` (non-empty — every transaction must be attributable to at least one concept).
    - `lib/schemas/scoring.ts` (Unit 6's file): `ScoringResultSchema` gained `concept_results: { concept_tag, result: 'pass' | 'fail' }[]`, computed deterministically in `score-submission.ts`'s new `computeConceptResults` (no LLM judgment, same determinism boundary as the rest of that file) — a transaction "passes" for its concept(s) only if every scored field diffed for that transaction is correct; a concept tagged on more than one transaction in an exercise fails overall if any occurrence fails.
    - `lib/llm/prompts/diagnostic-exercise.ts` (Unit 4's file): JSON Schema and system prompt updated so newly-generated diagnostics populate `concept_tags`. The JSON Schema itself was extracted into a new shared `lib/llm/prompts/exercise-json-schema.ts` so the diagnostic and adaptive prompts (below) can't drift out of sync with each other.
    - `lib/db/queries/exercises.ts` (Unit 4's file): `ExerciseKind` extended to `'diagnostic' | 'adaptive'`; added `getLatestExercise` (latest exercise of either kind — needed because the "current" exercise is no longer always the diagnostic). `app/(chat)/chat/actions.ts`'s `submitFiles` and `requestHint` switched from `getLatestDiagnosticExercise` to this new function; `confirmWalkthrough` still uses the diagnostic-specific fetch since it's fetching the diagnostic it just generated.
  - **Data model** (new migration `supabase/migrations/20260813150000_mastery_engine.sql`, not yet applied to the live project):
    - Alters `exercises`'s `kind` check constraint to add `'adaptive'`.
    - `concept_attempts` (append-only, RLS select-only — written only via service-role from the recompute step), `concept_mastery` (RLS select-only, no insert/update policy for `authenticated` — same pattern as `exercises.answer_key`/`scoring_results`, all writes go through the service-role client), `company_ledger_registry` (unique on `learner_id, ledger_name`), `company_transaction_log` — all four scoped `auth.uid() = learner_id`, matching every prior unit's RLS convention. **Not yet applied or RLS-tested against the live Supabase project.**
  - `lib/schemas/state-patch.ts` — new file, `StatePatchSchema` (`concept_mastery_deltas`, `escalation_changes`). Not an LLM-validated schema like the others (`mastery.ts` is pure deterministic logic, not an LLM call) — exists to keep the recompute step's output shape explicit and typed.
  - `lib/tutor/mastery.ts` — new file, pure functions only, no DB write (invariant 5): `recomputeMastery` (derives `concept_mastery` deltas + escalation flags from the full `concept_attempts` history — idempotent, confirmed by test), `checkReinforcement` (2-of-last-3-failed), `selectWeakConcept` (reinforcement-active concept first, then escalation-active, then lowest-status not-yet-mastered — returns `null` once everything is mastered, which Unit 13/14 will act on). `CLEAN_HINT_RUNG_THRESHOLD = 4` as a named constant per the spec's explicit instruction — a pass with average hint rung ≥4 doesn't count toward the mastery streak. Escalation activates on 3 total failures within a 5-attempt lookback window (not necessarily consecutive, per `project-overview.md`'s framing, distinct from reinforcement's stricter 2-of-3 window) — chose a bounded lookback so a learner who failed 3 times long ago and has since mastered the concept doesn't stay escalated forever; not explicitly specified, flagged as an assumption.
  - `lib/db/queries/mastery.ts` — new file: `insertConceptAttempts`, `getConceptAttempts` (full history, oldest-first), `getConceptMasteryMap`, `applyStatePatch` (the one sanctioned `concept_mastery` write path — upserts on `learner_id, concept_tag`, confirmed by grep that no other write path exists anywhere in the codebase), `getModuleNumber`.
  - `lib/db/queries/company.ts` — new file: `getCompanyLedgerRegistry`, `registerCompanyLedgers` (upsert with `ignoreDuplicates: true` — reusing an existing name on purpose shouldn't error), `getRecentCompanyTransactionLog` (bounded to the last 10 entries — `TRANSACTION_LOG_SLICE_SIZE` — so the generation prompt doesn't grow unboundedly as history lengthens), `appendCompanyTransactionLog`.
  - `lib/llm/prompts/adaptive-exercise.ts` — new file, `buildAdaptivePrompt`/`buildAdaptiveRetryPrompt`. System prompt includes: the target concept tag and an explicit instruction that the scenario must genuinely exercise it; the difficulty level; an escalation-mode block (only included when `escalation_active` — instructs slower pacing/more scaffolding, explicitly "more hand-holding, not harder," per the spec); and the full company-awareness block (ledger registry + recent transaction log, or a "first exercise, no history yet" message when both are empty) with an explicit instruction never to reintroduce an existing name with conflicting details.
  - `lib/tutor/generate-exercise.ts` — added `generateAdaptiveExercise`: fetches company context, calls the adaptive prompt with bounded 3-attempt retry (same pattern as `generateDiagnosticExercise`), persists via `insertExercise('adaptive', ...)`, then registers any new ledgers and appends the transaction summary to the company log — this ordering (persist first, then register) is what keeps the registry accurate for the _next_ call. `dropOneLevel` helper (floored at L0) applies the reinforcement level-drop.
  - `lib/llm/tracing.ts` — `CallType` extended with `'adaptive-generation'`, tagged with `targetConceptTag`/`reason` via `extraMetadata` (the same optional field Unit 8 added for hint rungs).
  - `lib/jobs/run-scoring.ts` (Unit 7's file) — two new steps added after `persist-scoring-result`: `recompute-mastery` (logs one `concept_attempts` row per concept this exercise covered, using the exercise's overall hint depth via `getHintDepthForExercise` — hints in this product are exercise-scoped, not concept-scoped, so this is a deliberate simplification; then re-derives and applies the full `concept_mastery` patch) and `generate-next-exercise` (picks a weak-concept target via `selectWeakConcept`, derives a base difficulty level one step above the just-scored exercise's level via `deriveBaseDifficultyLevel`, capped at L4, then calls `generateAdaptiveExercise` — which applies the reinforcement drop on top if applicable). If `selectWeakConcept` returns `null` (everything mastered), this step is a no-op — confirmed by code review that no AIA-transition/capstone logic exists yet, correctly deferred to Units 13/14. This satisfies the spec's "triggers automatically... the learner doesn't have to ask" requirement, same auto-delivery pattern as the Unit 4 diagnostic.
  - **Chat UI delivery** (`app/(chat)/chat/`): `actions.ts` gained `getNextExercise` (fetches the learner's latest exercise if it differs from the one just submitted against — the adaptive-generation job step has already run server-side by the time the submission's Realtime status flips to `'scored'`, so the fetch is synchronous from the client's perspective, no polling). `PendingSubmission.tsx` calls it immediately after successfully fetching scored feedback, and reports the new exercise upward via a new `onNextExercise` callback. `ChatShell.tsx`'s `handleNextExercise` replaces the active `exercise` state, resets the per-exercise `submissionMessages` timeline and `hasRequestedHint` state (a new exercise has its own independent hint-rung count, same isolation Unit 8 already guaranteed at the query level). `message.ts` gained an optional `progressLabel` field on `ChatMessage`; `MessageBubble.tsx` renders it as a small inline badge using only existing token styles (`bg-bg-canvas`/`text-text-secondary`), no new component — per the spec's explicit "no major new UI" instruction.
  - Dependencies: none new — built entirely on Units 4/6/7/8's existing LLM/Zod/Langfuse/Inngest infrastructure, per spec.
  - User confirmed live, full spec checklist:
    - [x] Apply `20260813150000_mastery_engine.sql` to the live Supabase project
    - [x] RLS direct test on all four new tables (`concept_attempts`, `concept_mastery`, `company_ledger_registry`, `company_transaction_log`)
    - [x] Live click-through: diagnostic scored → next exercise is adaptive, not the diagnostic again, and targets an actual weak concept from the diagnostic's results
    - [x] Simulate 3 consecutive clean passes on one concept in the live environment and confirm `concept_mastery.status` reaches `'mastered'`
    - [x] Confirm a rung-4/5-heavy pass does NOT advance the mastery streak, live (not just in the unit test)
    - [x] Simulate 2-of-3 failures on a concept and confirm the next exercise for it drops a difficulty level
    - [x] Simulate 3 failures and confirm `escalation_active: true` plus visibly more-scaffolded prompt content in the next generated exercise
    - [x] Generate 3-4 adaptive exercises in sequence and confirm no ledger/party name collision against `company_ledger_registry`
    - [x] Confirm `company_transaction_log` accumulates correctly across multiple exercises
    - [x] Langfuse trace check for `adaptive-generation` calls, tagged with target concept
    - [x] Confirm next-exercise generation is fully automatic in the live UI (learner never has to ask)
    - [x] `npm run build` passes with no TypeScript errors

- **Unit 10 — Source document generation** (spec: `context/specs/10-source-document-generation.md`), `npm run build` + `npm run lint` + `npm test` (Vitest, 40/40 passing, unchanged from Unit 9 — this unit's own new logic (document-name derivation, the PDF templates, the generation/render pipeline) is LLM-generation/rendering plumbing, not tested with a dedicated file, matching the established precedent that Units 4/6/8/9's own generation-call plumbing wasn't unit-tested either, only exercised live) all pass clean. Fully verified working end-to-end by user against the spec's complete checklist: migration applied to the live Supabase project, RLS confirmed by direct test, and live document generation/rendering confirmed for both `vendor_invoice` and `bank_statement`.
  - **Ambiguity flagged and resolved with the user before writing code**, per `ai-workflow-rules.md` Section 4: the spec says source documents are "driven by module/difficulty configuration," but no such config exists anywhere in the codebase — Unit 9 established that module number is _derived_ (mastered-concepts + 1), not a stored curriculum table, so there was nothing to hang a difficulty/module → "requires a document" rule on without inventing new structure the spec doesn't ask for. Asked the user, who confirmed: the LLM decides per transaction during exercise generation (not a hardcoded level threshold), and further confirmed the LLM also picks the document type (`vendor_invoice`/`bank_statement`) and count freely per exercise (zero or more), rather than being capped at one document per exercise.
  - **Cross-boundary schema change, flagged before making it** (same pattern as Units 6/9's answer-key extensions): `AnswerKeyEntrySchema` (`lib/schemas/exercise.ts`, Unit 4's file) gained `requires_source_document: boolean` and `source_document_type: SourceDocumentType | null` — the LLM's per-transaction decision, both fields present-and-explicit (never omitted), same discipline as this schema's other nullable fields. `lib/llm/prompts/exercise-json-schema.ts` (Unit 9's shared JSON Schema, used by both diagnostic and adaptive prompts) updated in the same change so the two never drift.
  - `lib/schemas/source-document.ts` — new file, Zod schemas for document _content only_, not layout: `VendorInvoiceContentSchema` (`vendorName`, `vendorGSTIN`, `invoiceNumber`, `invoiceDate`, `lineItems[]`, `taxBreakup` (raw CGST/SGST/IGST amounts as printed, not a classification), `totalAmount`) and `BankStatementContentSchema` (`accountHolderName`, `period`, `transactions[]` with `date`/`narration`/`debit`/`credit`/`balance`). `GeneratedSourceDocumentSchema` is a discriminated union on `doc_type` so a mismatched content/type pairing fails validation rather than silently coercing.
  - `lib/llm/prompts/source-document.ts` — new file, `buildSourceDocumentPrompt`/`buildSourceDocumentRetryPrompt`, one JSON Schema per `doc_type`. Grounds the document's figures in the specific answer-key entry it represents (amount, GST/TDS, bill reference) so a learner correctly reading the document arrives at exactly the posting the answer key expects. Carries an explicit, separately-named `CONTENT_BOUNDARY_INSTRUCTION` block: the document must show only raw commercial facts a real vendor/bank would print, and must never state or imply the accounting classification (e.g. never label a GST amount "IGST Payable" or say which ledger to post to) — the spec's explicit "keep this boundary in the prompt, not just assumed" instruction.
  - `lib/documents/generate-source-document.ts` — new file, `generateSourceDocument`: the new `source-document-generation` LLM call type, same bounded 3-attempt retry pattern as every other generation call in this codebase, validated against `GeneratedSourceDocumentSchema`, throws (never persists/renders unvalidated output) on exhaustion. `lib/llm/tracing.ts`'s `CallType` extended with `'source-document-generation'`, tagged via `extraMetadata` with `docType`/`transactionSequence`.
  - `lib/documents/templates/vendor-invoice.tsx` and `lib/documents/templates/bank-statement.tsx` — new files, `@react-pdf/renderer` components. Purely deterministic: same validated content object always produces the same rendered PDF, confirmed by inspection that neither file makes any LLM/network call — they only consume the already-validated `VendorInvoiceContent`/`BankStatementContent` props. Styled with the `ui-context.md` neutral-scale hex values directly (react-pdf has no access to the app's Tailwind/CSS-variable token system, so the light-mode hex values are hardcoded per style, documented inline).
  - `lib/documents/render-source-document.ts` — new file, `renderSourceDocumentPdf`: dispatches on `doc_type` to the matching template and calls `@react-pdf/renderer`'s `renderToBuffer`, returning a `Buffer`. The one deterministic seam between validated LLM content and PDF bytes.
  - `supabase/migrations/20260814120000_exercise_source_documents.sql` — new `exercise_source_documents` table (`id`, `exercise_id` references `exercises`, `doc_type` check-constrained to `'vendor_invoice' | 'bank_statement'`, `storage_path`, `structured_data` jsonb — learner-facing content, safe to store plainly per the spec, unlike `answer_key`; `created_at`). RLS: only a `select` policy, scoped via an `exists` subquery joining through `exercises.learner_id` (this table has no `learner_id` column of its own, per the spec's explicit design) — no `insert`/`update` policy for `authenticated`, all writes go through the service-role client, same pattern as `exercises.answer_key`. Private `exercise-documents` Storage bucket with a `select` policy scoped via `(storage.foldername(name))[1] = auth.uid()::text`, enforcing the spec's `exercise-documents/{learner_id}/{exercise_id}/{doc_id}.pdf` path convention, same discipline as the Unit 5 `submissions` bucket. **Not yet applied to the live Supabase project — the user needs to apply this migration before any of this unit's live checklist items can be verified.**
  - `lib/db/queries/source-documents.ts` — new file: `insertSourceDocument` (service-role write), `getSourceDocumentsForExercise` (fetches every document for an exercise, derives the client-facing `documentName` from `structured_data` — e.g. "Invoice — Parekh Integrated Services.pdf" — per the spec's card-label example), `getSignedSourceDocumentUrls` (resolves each document's `storage_path` to a time-limited signed URL, using the **authenticated** client rather than service-role — `exercise-documents`'s Storage select policy is scoped to `auth.uid()` via `storage.foldername`, so this call naturally enforces the same learner-scoping as every other Storage read in this codebase, rather than adding a separate application-level check; `SIGNED_URL_TTL_SECONDS = 3600`).
  - `lib/tutor/generate-exercise.ts` — added `generateAndAttachSourceDocuments` (private helper), called from both `generateDiagnosticExercise` and `generateAdaptiveExercise` immediately after `insertExercise`: filters the just-generated `answer_key.entries` down to ones flagged `requires_source_document: true`, and for each, calls `generateSourceDocument` → `renderSourceDocumentPdf` → uploads to `exercise-documents/{learnerId}/{exerciseId}/{docId}.pdf` → `insertSourceDocument`. Awaited by the caller — a document generation/render/upload failure propagates and fails the whole exercise-generation call rather than silently delivering an exercise whose promised document never arrives. `lib/llm/prompts/diagnostic-exercise.ts` (Unit 4's file) updated so the diagnostic explicitly always sets `requires_source_document: false` (the diagnostic is a fixed direct-entry template, per its existing spec) — only the adaptive path (`lib/llm/prompts/adaptive-exercise.ts`, Unit 9's file) actually instructs the LLM to use its judgment per transaction, per the user's confirmed design.
  - **Chat UI delivery** (`app/(chat)/chat/`): `message.ts` gained an optional `sourceDocuments` field on `ChatMessage` (only set on `kind: 'exercise'`), each entry carrying a signed `url`, never the raw `storage_path`. `DocumentCard.tsx` — new presentational component: file icon (inline SVG), document name, `bg-surface`/`radius-md` per the spec's design note, "View" action opening the signed URL in a new tab — a card, not an inline PDF preview, consistent with how a real chat product handles attachments. `MessageBubble.tsx` renders the card list below the scenario/content text, inside the same message bubble — never a separate message, per the spec. `actions.ts` gained a shared `getExerciseSourceDocuments` helper (fetch + resolve signed URLs) called from both `confirmWalkthrough` (initial diagnostic delivery) and `getNextExercise` (auto-delivered adaptive exercises); `page.tsx` resolves the same for the initial page-load exercise. `ChatShell.tsx`/`PendingSubmission.tsx` thread the resolved `sourceDocuments` array through `handleNextExercise`'s new parameter alongside the existing `hintDepth`/`moduleNumber`, and `ChatShell` holds it in its own `sourceDocuments` state, separate from `exercise` state, so a fresh exercise's documents replace the prior exercise's cleanly.
  - Dependency added: `@react-pdf/renderer` (first point it's actually needed, per the project's lazy-plumbing pattern — `architecture.md`'s stack table already named it as a Unit 10 dependency).
  - `architecture.md` updated in the same change: Section 2's folder listing gained `/lib/documents` (`generate-source-document.ts`, `render-source-document.ts`, `templates/`), and the PDF generation stack row confirms `@react-pdf/renderer` as the chosen library (over `pdf-lib`).
  - User confirmed live, full spec checklist:
    - [x] Apply `20260814120000_exercise_source_documents.sql` to the live Supabase project
    - [x] RLS direct test on `exercise_source_documents` (cross-learner select denial, joined through `exercises.learner_id`)
    - [x] Cross-learner Storage access test on the `exercise-documents` bucket, done directly (not assumed)
    - [x] Live click-through: an adaptive exercise whose answer key flags a transaction `requires_source_document: true` actually generates and attaches a PDF, for both `vendor_invoice` and `bank_statement`
    - [x] Spot-check a rendered invoice's total against the underlying transaction it represents (figures match `structured_data`/the answer key exactly)
    - [x] Manually confirmed no generated document states an accounting classification (no ledger name, voucher type, or tax-head label anywhere in the PDF)
    - [x] Confirmed regenerating a PDF from the same structured data produces the same layout (rendering is code-based, not LLM-based)
    - [x] Document card renders correctly in the chat message (icon, name, `bg-surface`/`radius-md`), in both light and dark mode, and the "View" action opens/downloads correctly
    - [x] Confirmed `answer_key`'s raw object never appears in any client-facing response related to document generation — only `structured_data` (via `getSourceDocumentsForExercise`) ever reaches the client
    - [x] `npm run build` passes with no TypeScript errors
    - [x] Confirmed no qualitative scoring, multi-part submission, or anomaly-seeding logic was accidentally introduced — this unit stayed scoped to document generation/rendering/delivery only (Unit 11 is separate)

- **Unit 2 manual verification** — user tested the core flow live and confirmed it works: sign-up (wrong-password error shown correctly), email confirmation via `/auth/callback`, landing on `/dashboard`, and log-in all fired successfully in the dev server logs. Still outstanding before Unit 2 can be marked fully done per `ai-workflow-rules.md` Section 7:
  - [x] Sign up with email + password → confirmation email flow → lands on `/dashboard`
  - [x] Log in with correct email + password → lands on `/dashboard`
  - [x] Log in with wrong password → inline error shown ("Invalid login credentials"), no crash
  - [ ] Refresh on `/dashboard` keeps session (cookie-based, no logout on reload)
  - [ ] Visiting `/dashboard` logged out → redirects to `/login`, never renders protected content
  - [ ] Logout button → returns to `/login`, `/dashboard` inaccessible until logging in again
  - [ ] No console errors across the full sign-up/login → dashboard → logout cycle (browser console, not just server logs)

## Next Up

- Unit 13 — AIA transition + AIA-assisted exercises (Navattic tour). Depends on Unit 9, Unit 12 (code-complete, pending live verification — see Unit 12 entry).
- Unit 14 — Capstone + diagnostic re-run + certificate. Depends on Unit 13, Unit 10, Unit 12.

## Open Questions

- None currently open.

## Architecture Decisions

- **Unit 12 (2026-08-17):** module-advancement rule confirmed as the spec's own stated assumption — a learner advances `module_progress.current_module` once every concept tagged to the current module (via the new `CONCEPT_TO_MODULE` map) is `'mastered'` with no active escalation. `context/specs/00-build.md` was checked first and does not resolve this either (same pattern as Unit 11's own flagged-assumption check) — confirmed directly with the user before writing any code, per the spec's explicit "confirm this against the original build spec before relying on it" instruction and `ai-workflow-rules.md` rule 14.
- **Unit 12 (2026-08-17):** the concept-to-module grouping (`CONCEPT_TO_MODULE` in `lib/schemas/exercise.ts`) is one concept per module, in `CONCEPT_TAGS` declaration order — user's choice when asked, over inventing a thematic grouping (e.g. bundling the 6 voucher-basics concepts into one module). No prior unit defined any such grouping; Unit 09's "module number" is a derived progress-label value (mastered-count + 1), a different concept from this unit's stored `module_progress.current_module`.
- **Unit 11 (2026-08-17):** the spec's own flagged open assumption — review-packet-in-chat vs. live-company export for the ledger-review exercise — is resolved as **review-packet-in-chat**, the spec's default build target. `context/specs/00-build.md` was checked and is silent on the mechanics, so it didn't resolve this either; confirmed directly with the user before writing any code, per the spec's explicit "confirm this against the original spec before building" instruction.
- **Unit 11 (2026-08-17):** `exercises.kind` extended to include `'explain'` and `'review'` (rather than reusing `'adaptive'` with a required-parts flag alone) — user's choice when asked, since `AnswerKeyEntrySchema`'s Tally-posting fields have no natural meaning for a review packet, and stretching that schema to cover a case it wasn't built for would be worse than adding two kind values with their own generation paths. A `'review'` exercise's answer-key equivalent is `ledger_review_items`, not `answer_key` (which persists an empty `entries` array for these rows). A follow-up question confirmed `'explain'` exercises ground their qualitative scoring in the exercise's own `answer_key` (reused, not a new hand-authored explanation-key field) — user's choice, since the answer key already carries what the explanation needs to be graded against.
- **Unit 11 (2026-08-17):** qualitative scoring is stored as a nullable `scoring_results.qualitative_score` column alongside the existing quantitative columns, not a separate parallel table — user's choice when the `NOT NULL` constraint on `weighted_score`/`tb_tie_out` turned out not to fit a review exercise's answer-key-less case. For a review exercise, `weighted_score`/`tb_tie_out` are persisted as `0`/`true` purely to satisfy the existing constraint, and `overall_result` is derived from `qualitative_score` alone; `combineOverallResult` (`lib/tutor/score-qualitative.ts`) takes the worse of quantitative/qualitative when both apply.
- **Unit 11 (2026-08-17):** real entries pulled into a review packet from `company_transaction_log` are always presented as clean (`is_anomaly: false`) — flagged as an assumption, not confirmed with the user, since this codebase has no "uncorrected past error" tracking on that table to draw a genuine anomaly from. The spec's "some genuinely fine, some — if an uncorrected past error exists — genuinely anomalous" is honored as written; there's just currently nothing that qualifies. Revisit if a later unit adds error tracking to `company_transaction_log`.
- **Unit 10 (2026-08-14):** the spec's "driven by module/difficulty configuration" trigger for source documents is implemented as an LLM per-transaction judgment call during exercise generation, not a hardcoded difficulty-level threshold or a new curriculum config table — user's explicit choice when asked, since Unit 9 already established that module number is derived rather than stored, and a new config table would be more structure than the spec actually calls for. The user also confirmed the LLM freely picks document type (`vendor_invoice`/`bank_statement`) and count (zero or more) per exercise, rather than being capped at one document per exercise.
- **Unit 9 (2026-08-13):** `concept_tag` vocabulary and source — user confirmed the LLM assigns `concept_tags` per answer-key entry from a new fixed vocabulary (`CONCEPT_TAGS` in `lib/schemas/exercise.ts`), rather than reusing `scoring.ts`'s generic `SCORED_FIELDS` (account/gst/tds/etc.) as a stand-in for accounting concepts. This is a cross-boundary extension of Unit 4's `exercise.ts` and Unit 6's `scoring.ts`, same pattern as Unit 6's own answer-key extension — flagged to and confirmed by the user before writing code.
- **Unit 9 (2026-08-13):** "module number" for the progress label is derived (count of mastered concepts + 1, `getModuleNumber`), not a stored/manually-advanced column — user confirmed, since no prior spec defines a concrete module-numbering scheme and inventing a concept→module grouping table would be more structure than currently specified.
- **Unit 9 (2026-08-13):** the "persistent vs. fresh Tally company per module" question blocking Unit 9 is resolved — user confirmed treating the spec's stated design constraint (`context/specs/09-mastery-engine-adaptive-generation.md` line 7: one single persistent Tally company for the learner's entire journey, confirmed, may change later) as the answer, rather than reopening it as a fresh decision. This is why exercise generation in this unit is company-aware (`company_ledger_registry`, `company_transaction_log`) — every new exercise must be generated with awareness of everything already posted in that one company.
- **Unit 7 (2026-08-13):** live client updates use Supabase Realtime (Postgres Changes on `submissions`/`scoring_results`), not Inngest's own realtime/streaming feature — the spec's explicit call, kept because it cleanly separates concerns already established in `architecture.md`: Inngest owns job durability, Supabase Realtime owns DB-change delivery to the client, and both ride the same RLS enforcement already in place rather than introducing a second authorization surface.
- **Unit 6 (2026-08-12):** `AnswerKeyEntrySchema` (Unit 4's `lib/schemas/exercise.ts`) extended with `voucher_type`, `gst_head`, `gst_rate`, `tds_section`, `tds_rate`, `tds_base`, `bill_reference`, `narration` — a cross-boundary touch into a prior unit's file, flagged to and confirmed by the user before making it, since the Unit 4 schema didn't carry the fields Unit 6's spec needs to diff against. The diagnostic exercise prompt was updated in the same change so newly-generated exercises populate these fields (nulled for L0's no-GST/TDS single-concept transactions).
- **Unit 6 (2026-08-12):** GST/TDS signal on the _parsed submission_ side (as opposed to the answer key) is inferred from ledger name text patterns in `score-submission.ts`, not extracted as structured fields by Unit 5's parser — user's choice when asked. Keeps Unit 5's XML parsing generic/structural; the accounting-domain interpretation of "which ledger is a GST/TDS line" lives in the scoring file, consistent with the spec's instruction to keep correctness logic deterministic and centralized in `score-submission.ts`.
- **Unit 6 (2026-08-12):** the Karbon VA House Practices Rulebook referenced by `project-overview.md` goal 6 and the Unit 6 spec's "Rulebook grounding" doesn't exist as ingestible content anywhere in this codebase yet. A static placeholder grounding block lives in `lib/llm/prompts/coaching.ts` until real ingestion exists — consistent with this project's lazy-plumbing pattern (LLM/Zod/Langfuse in Unit 4, PDF lib deferred to Unit 10, job runtime deferred to Unit 7). Whichever unit eventually ingests the real Rulebook should replace this placeholder, not restructure the prompt.
- **Unit 5 (2026-08-12):** Vitest introduced (dev dependency) as the first automated test runner used in this project — `architecture.md`'s stack table already named it, but no unit had needed it until parsing/validity-gate logic existed. `vitest.config.ts` mirrors `tsconfig.json`'s `@/*` path alias. Tests are colocated per code-standards rule 22 (`daybook.test.ts` beside `daybook.ts`, etc.), run against the real sample XML files rather than synthetic-only fixtures.
- **Unit 5 (2026-08-12):** the two files a learner attaches (Day Book, Trial Balance) are disambiguated in the composer via two explicit labeled attach slots, not filename-sniffing — asked the user since the spec didn't specify; user chose explicit labeled slots.
- **Unit 4 (2026-08-12):** `lib/supabase/service-role.ts` introduced as the first use of the service-role Supabase client, invoked from a Server Action (`app/(chat)/chat/actions.ts`) rather than only an API route or background job as `architecture.md` Section 4 originally implied — that section's wording was broadened in the same change to explicitly include Server Actions. Chosen because `exercises.answer_key` needs a server-side writer and the `exercises` table intentionally has no `insert` RLS policy for `authenticated` (learners must never be able to write their own answer key).
- **Model choice for Unit 4:** `OPENROUTER_MODEL` defaulted to `openai/gpt-4o-mini` (2026-08-12), user's choice when asked — confirmed OpenRouter structured-output (`response_format: json_schema`) support. Can be swapped later via env var alone, no code change needed.
- **`ai-thinking` design token** (defined in `ui-context.md` but never added to `tailwind.config.ts`/`globals.css` in Unit 1) was added in Unit 4, the first unit to actually need it, rather than upfront in Unit 1 — consistent with the project's "introduce plumbing lazily, at first point of need" principle already applied to LLM/Zod/Langfuse.

- **Unit 2 auth method changed from Google OAuth + magic link to email + password** (2026-08-12), by explicit user request during implementation. Updated in the same change: `context/specs/02-auth.md`, `context/specs/00-build.md` (Unit 2 line), `context/project-overview.md` (sign-up step, Onboarding & Access feature list), `context/architecture.md` (auth stack row, Section 4 auth model). Reason given: avoided needing a working Google Cloud OAuth app (the user had pasted an email/password-style credential into the Google Client ID field, not a real OAuth client) — password auth needs no external provider setup, only Supabase's built-in Email provider. Google OAuth and magic link can be added back later as additional options without restructuring, since both routed through the same `/auth/callback` code-exchange pattern already in place.
- Build order and dependency graph fixed at 14 units (see `context/specs/00-build.md`). No Sentry, no Clerk in v1.
- Plumbing is introduced lazily, at first point of need, not upfront: LLM/Zod/Langfuse in Unit 4, PDF lib in Unit 10, job runtime (Inngest/Trigger.dev) in Unit 7, Navattic in Unit 13.
- Unit 6's scoring call starts as an inline blocking call and is retrofitted to a durable job in Unit 7, rather than building durability upfront.

## Session Notes

- Unit 5 (2026-08-12): the spec's real sample files (`DayBook.xml`, `TrialBal.xml`) weren't found by an initial targeted search of `context/` and the repo tree; asked the user, who said they were "already there" — a broader filesystem search found both at the project root (sibling to `package.json`), not documented anywhere in `context/`. Used directly as test fixtures rather than constructing synthetic ones. Confirmed by hex inspection: both are genuinely UTF-16LE with a `FF FE` BOM, matching the spec's claim.
- Unit 4 (2026-08-12): Langfuse is self-hosted at `langfuse.prod.korefi.ai` (set via `LANGFUSE_BASE_URL`), not the default `cloud.langfuse.com` — `.env.example` documents `LANGFUSE_BASE_URL` as a required override, not just an optional default. User confirmed the full spec verification checklist passed on first try, no fixes needed this round.

- Unit 1 built and verified end-to-end (2026-08-12). `create-next-app` defaults to Tailwind v4 (CSS-first `@theme` config); since the spec explicitly requires tokens in `tailwind.config.ts`, used Tailwind v4's `@config` directive to keep a classic JS/TS config file rather than downgrading to v3 (user's choice when asked).
- Next.js's `agentRules` dev feature auto-appends a block to `CLAUDE.md` on every `next dev` start; disabled via `agentRules: false` in `next.config.ts` to avoid unexpected drift in the user's project instructions file.
- Unit 2 (2026-08-12): the Supabase MCP connector ("Supabase VA") available in this environment is hard-wired to a pre-existing, unrelated project (`kxlrxqewcnwcstzvyrcr`) belonging to a different app (client/VA management — `clients`, `invoices`, `document_uploads`, etc.), not a fresh AIA Academy project. It also has 2 tables (`client_contacts`, `addition_charges`) with RLS disabled, unrelated to this build — flagged to user, not touched. User confirmed they'd already created a separate, correct `AIA Academy` Supabase project (`qshypptxkydplmyrxsou`) and pasted its URL + anon key directly since the MCP connector can't target it; `.env.local` was hand-updated to point there instead. No MCP tool access (list_tables, get_advisors, etc.) to the actual AIA Academy project during this session — only REST/auth via the anon key through the app code itself. Revisit whether a project-specific Supabase MCP connection should be set up before Unit 3, which needs `execute_sql`/migration tooling for the `learner_profile` table.
- User declined automated dev-server/browser verification for Unit 2 ("you dont test i will test maally") — will test manually across all cases and report results back. Don't proactively launch dev servers or drive the browser for auth verification going forward in this project; wait for the user's test report instead.
- During manual testing, "Continue with Google" correctly failed with `Unsupported provider: provider is not enabled` (Google provider wasn't turned on yet) — confirmed the app code path was working. While investigating, user attempted to fill Supabase's Google provider "Client IDs" field with an email/password-shaped credential instead of a real Google Cloud OAuth Client ID/Secret pair, which prompted the switch to email+password auth instead of debugging OAuth setup further.
- Hit a real bug after the email+password rewrite: `actions.ts` (a `'use server'` file) exported `initialAuthFormState`, a plain object constant, alongside the async Server Actions. Next.js requires `'use server'` files to export only async functions — non-function exports throw `A "use server" file can only export async functions, found object.` at runtime (not caught by `npm run build`, only surfaced when actually hitting `/login` in the dev server). Fixed by moving `AuthFormState`/`initialAuthFormState` into a new `app/(auth)/login/auth-form-state.ts` and importing from there in both `actions.ts` and `AuthForm.tsx`. Lesson: `'use server'` files need their state/types split into a separate plain module, not colocated even for small unit-local state.
- `proxy.ts` vs `middleware.ts`: user first asked to keep `middleware.ts` (declined the Next.js 16 codemod), later in the same session asked to "fix the migrate part" — confirmed via a clarifying question that this meant migrating to `proxy.ts` after all. Now on `proxy.ts` (function renamed `middleware` → `proxy`), deprecation warning gone. If asked again, check current state in the file first rather than assuming either direction.
- Also confirmed during this session: `npm run build`/`npm run lint` passing does not guarantee no runtime errors — the `'use server'` export bug and the stale-Turbopack-cache issue (`rm -rf .next` needed after significant file renames while `next dev` was running) both only surfaced by actually running the dev server. Static checks are necessary but not sufficient for this stack.

### 2026-09-02 (session 4) — Scorer fairness fixes surfaced by Garima's Level 2 preview

Previewing Garima's Level 2 exports (correct shape: 12 May vouchers, 69-row
ledger-wise TB, gate valid) against her patched key scored 74%/fail, but a
per-transaction diff showed her posting was largely right. Three scorer
defects in `lib/tutor/score-submission.ts` were penalising correct work and
would have hit every intern on generated (multi-leg) keys:

1. **GST expectation read from the wrong leg.** Generated keys carry
   `gst_head`/`tds_section` only on the tax leg; the party leg says null.
   `diffVoucher` used `expectedLegs[0]` for the whole transaction, so every
   correct GST posting was `GST_UNEXPECTED` (6 of 6 taxed transactions). Now
   the transaction's tax expectation is whichever leg states one.
2. **Annotated bill references never matched.** Keys store
   `"BR-205 (New Ref)"` / `"Against DT-114 (Partial)"`; `diffBillReference`
   compared the whole string, so a learner's correct `BR-205` allocation was
   `BILL_REFERENCE_WRONG` (7 false flags). Parentheticals and the `Against`
   prefix are stripped before comparison; genuinely wrong refs still fail.
3. **GST ledger naming penalised on the tax leg.** Tally/pack ledgers are
   plain `IGST`/`CGST`/`SGST`; keys say `Output IGST`/`Input CGST`. A 4-letter
   name can never clear the 5-char containment floor, so the tax leg was
   `ACCOUNT_WRONG`. `accountNamesMatch` now treats names sharing the same GST
   head token as a match (input/output side is still judged by `diffGst`).

Result on the real files: Garima 74% → 94% (remaining flags are genuine:
2 bank↔cash moves posted as Payment instead of Contra, `Petty Cash` ledger
instead of `Cash`, MS-331 posted as 49,999 + IGST 1, `INV-011`/`DT-115` refs
instead of `BR-205`/`DT-114`); Praveen 85% → 95%. Tie-out still false for
both because uncorrected April balances carry forward (Office Equipment,
Sales Returns netted into Sales, Bank Charges, Software Subscription) — by
design of the one-continuous-books model. Regression tests added under
"scorer fairness fixes from Garima Level 2".

### 2026-09-02 (session 5) — Garima Level 3 verified; multi-rate GST consolidation fix

Garima uploaded Level 2 (scored 95%/partial with the fixed scorer) and Level 3
(`explain`, June 2026, 12 tx) generated at 07:51 UTC. Verified against DB:
all 12 transactions balanced; till opened at -200 and Tx 1 is the forced
Contra withdrawal (cash never negative afterwards); 35 opening balances match
the netting of her diagnostic + Level 2 keys (tax ledgers excluded by design);
all 4 vendor invoices (MS-2201, DT-889, CW-771, VV-556) match the key totals,
tax heads and June dates; the bank statement is pinned to Blossom Retail Pvt
Ltd and its 3 lines match the key receipts/payment exactly.

One scoring risk found and fixed: Tx 7 (Mysore Decor, 9%+9% furniture and
6%+6% packing on one invoice) is stored as two CGST legs + two SGST legs, but
Tally shows ONE combined CGST/SGST line per voucher. The natural posting
scored 76% with two false ACCOUNT_WRONGs. `diffVoucherAgainstAnswerKey` now
consolidates expected legs and posted ledger entries by (account, side)
before leg matching; both consolidated and split postings score 100%, and
the CGST-twice/SGST-missing case is still caught. Real-file previews
unchanged (Garima L2 94%, Praveen L2 95%). Tests: 209 passing.

Cosmetic, not scored: the generated bank statement's running balance column
(1.25L → 1.27L) and its narration refs (N26050108…) do not match the key's
narration UTRs (UTR7788392…) or the true HDFC balance (~17.9L). Narration is
scored for presence only, so no learner impact; candidate for a later
statement-grounding pass.

### 2026-09-02 (session 6) — Praveen's two Level 2/3 complaints → two generator guardrails

1. **Bank statement must show what the key expects (invisible bill reference).**
   Praveen's Level 2 key demanded bill reference `KE/2026/018` (and
   `MA/2026/09`) on bank-backed transactions, but the statement lines only
   read "NEFT/…/KARNATAKA EMPORIUM/RCP" and the question text carried no
   number — the reference existed nowhere the learner could see, so his
   correct allocation against the open April invoices was
   BILL_REFERENCE_WRONG and the coaching quoted the invisible number back.
   New `checkBankStatementContent` in `lib/documents/generate-source-document.ts`
   (mirrors `checkVendorInvoiceContent`): every key transaction must map to
   exactly one statement line on the correct side (debit/credit derived from
   the bank leg's direction), for the exact amount, on the transaction's own
   date, whose narration contains the bill reference (annotations and the
   "Against" prefix stripped). Violations feed the existing retry loop. The
   statement prompt now states the reference-in-narration requirement.
2. **Scenario text duplicating the transaction list.** The chat renders
   scenario + structured transactions as a numbered list; Praveen's Level 3
   scenario prose also contained the 12 numbered items ("Post all twelve…"),
   so he saw them twice. New `stripDuplicateTransactionList` in
   `lib/tutor/generate-exercise.ts` removes numbered scenario lines that
   restate a transaction (prefix/equality match on normalised text), applied
   after validation and before documents/insert; the adaptive prompt now says
   the list lives ONLY in the `transactions` array.

Regression tests added for both (`checkBankStatementContent`,
`stripDuplicateTransactionList`). Praveen's already-scored Level 2 is left
as is (99%, two unfair bill-reference flags explained to him); both Level 3
statements were verified to carry their references, so no live exercise
needed patching.

Follow-up (same session): the new statement check's test caught an ordering
bug shared with the morning's scorer fix — bill-reference annotations were
split on commas BEFORE the parenthetical was stripped, so a key like
`KE/2026/018 (part payment, ₹30,000 balance outstanding)` shredded into
non-references. Both `billReferenceTokens` (documents) and
`diffBillReference` (scorer) now strip parentheticals first, then split.
Regression test added to the scorer suite.

### 2026-09-02 (session 7) — Carried-forward openings double-counted April (Praveen's "-35,200 vs -80,100")

Praveen reported his Level 3 said the till was at -35,200 while his Tally
showed Cash Cr 80,100 (+ a 25,000 petty-cash ledger = -55,100). His books
were right; the key was wrong. `getExpectedOpeningBalances` /
`getExpectedCashPosition` summed EVERY prior key's opening_balances +
entries, but a generated key's openings are already the cumulative position
(stamped from the same function), so April was counted twice: both Level 3
keys opened with HDFC at ~18.2L instead of ~9.5L, Sales at 51L instead of
26L, Garima's till at -200 instead of -20,100 (31 of 35/36 accounts wrong).
The 2026-09-02 (session 5) verification of those openings used the same
flawed netting and therefore passed — a wrong check, corrected here.

Fix: `netAnswerKeys` in `lib/db/queries/company.ts` — a key's non-empty
opening_balances RESET the running position before its entries apply; keys
without openings add on top. `cashPositionFromNet` / `openingBalancesFromNet`
shape the two consumers off the same net. Unit tests in `company.test.ts`.

Live data: `Downloads/patch-level3-openings.sql` (backup JSON alongside)
rewrites both Level 3 keys' opening_balances to the true post-Level-2
position, raises Transaction 1's withdrawal so the till actually turns
positive (Garima 10,000 → 30,000, closing 9,900; Praveen 50,000 → 70,000,
closing 11,400 after his 3,500 cash payment), corrects the "minus Rs …"
figure in the prose, and strips Praveen's duplicated inline list. To be run
by the owner before either uploads Level 3.

### 2026-09-02 (session 8) — "Stock items are optional" note on every exercise

Praveen asked whether Level 3's itemised invoices require stock items. The
evaluator scores ledgers only, so `lib/chat/exercise-content.ts` now composes
the exercise message (scenario + items + a fixed note: stock items optional,
accounting mode is fine, evaluation checks ledgers/amounts/GST/bill
references). Both message builders (`build-timeline.ts` server timeline and
`ChatShell.tsx` live append) use it, so the text cannot drift. Deterministic
UI text, not a prompt change — applies to every exercise including the pack.

### 2026-09-02 (session 9) — Opening figures in the prose are now code-written

Yeshas's Level 2 prose said "Rs 8,67,186 in HDFC Bank" for a carried-forward
balance of 8,66,116 (the model mistyped a digit; scoring never reads the
prose, but the learner does). Three deterministic layers in
`lib/tutor/generate-exercise.ts`: `checkOpeningFigures` (a rupee figure in a
sentence about cash/bank/till/opening that isn't the real cash or bank figure
feeds the combined retry message), `scrubOpeningFigureSentences` (drops any
such sentence that survives via the fallback path) and `stampOpeningPosition`
(appends "Opening position for this batch (system-computed from your
books): Cash-in-Hand Rs …; HDFC Bank — 1234 Rs …", with "(overdrawn)" when
negative). The prompt now tells the model not to quote the figures. Live
fix: `Downloads/patch-yeshas-level2-prose.sql` corrects his stored prose.

Praveen's Level 3 export previewed against his patched key: 12 June
vouchers, 60-row TB, gate valid, 98% partial — the one flag is his own
"Office Maintanece" ledger spelling; Cash closes at 11,400 exactly as the
key expects (petty cash merged).

### 2026-09-03 — Multi-part scoring job never advanced the learner (no Level 4 after an explain batch)

Praveen's Level 3 (explain kind: Day Book + Trial Balance + explanation) was
scored 98% by `wait-for-submission.ts`, but that job persisted the score and
returned. Only `run-scoring.ts` (two-file path) logged concept attempts,
recomputed mastery/module progress and generated the next exercise — so the
first learner through an explain batch got feedback and then nothing.

Fix: the post-scoring pipeline now lives in `lib/jobs/advance-learner.ts`
(`logAttemptsAndClassifyRectifications`, `recomputeMasteryAndModuleProgress`,
`generateNextExercise`, plus `deriveBaseDifficultyLevel` /
`describeRectification`) and both jobs call it inside their own steps.
`generateNextExercise` is idempotent on the scored submission's created_at:
if any exercise newer than that already exists for the learner it returns
'already-exists', so a rerun or manual re-trigger can never issue two
batches for one submission. The multi-part job also now feeds rectification
descriptions into coaching and records the Langfuse submission score, same
as the two-file job. Praveen's stranded Level 3 is advanced by a one-off run
of the same three helpers against his submission.

### 2026-09-03 — Praveen advanced to Level 4; "Bank Charges" counted as the bank

One-off run of the shared helpers for Praveen's Level 3 submission: attempts
logged (payment_voucher_basics STILL_FAILING → escalation), mastery
recomputed (sales_voucher_basics mastered; module 2), Level 4 generated —
a 4-payment July drill, which is the escalation path by design (single
concept, exact count), not a 10–12 batch. Verified: 39 openings all correct
(Cash 11,400, HDFC 8,08,176), balanced, cash never negative, no inline list.

Root cause of the "8,67,186" figure finally found: `BANK_ACCOUNT_PATTERN`
(/\bbank\b|hdfc/) matched the "Bank Charges" expense ledger, so the bank
position fed to the prompt and stamped in the prose was inflated by
1,070.34 (Yeshas 8,66,116 → 8,67,186; Praveen 8,09,246 for 8,08,176). The
model had copied our number faithfully — the session-9 note blaming a
mistyped digit was wrong. `isBankLedger` now excludes charge/commission/
interest/fee/penalty ledgers. Answer keys were never affected. Live fix for
Praveen's stamped line: `Downloads/patch-praveen-level4-prose.sql`.

### 2026-09-03 — Explain/review exercises now say they need a typed part

The exercise message never said an explain batch has a third, typed part;
the only hint was the composer placeholder. `formatExerciseContent` now
takes the exercise's requiredParts and appends a fixed "This level has THREE
parts…" note for explain_text / review_text exercises (code-written, not a
prompt instruction). Both message builders pass requiredParts. Tests: 236.

### 2026-09-03 — Settlements must reference bills that exist (open-bills guard)

Praveen's Level 4 Tx 1 said "full settlement of DT-2216 for 42,500" — no
such bill; Deccan Traders' real open bill is DT/334 for 69,620. Yeshas's
Level 2 Tx 7/8 settled BR/S/098 and CS/612, neither of which exists. Same
class as the KE/2026/018 defect: a learner cannot allocate against a bill
Tally has never seen.

Fix: `openBillsFromKeys` / `getOpenBills` (company.ts) derive bill-by-bill
state from the answer keys (raises less settlements per party+ref; only
positive balances are open). The adaptive prompt now carries an OPEN BILLS
block (the only bills a settlement may name, with balances, plus the
advance/New Ref rule), and `checkSettlementReferences` (generate-exercise.ts)
rejects — into the combined retry — any receipt/payment whose reference is
neither an open bill of that party nor a bill raised earlier in the batch,
an amount above the balance, or a "full settlement" that doesn't equal it.
Tests: 242. Live data: `Downloads/patch-settlement-refs.sql` (Praveen Tx 1
→ DT/334 69,620; Yeshas Tx 7 → INV-022 part payment accepting the printed
BR/S/098 too; Tx 8 reworded as an advance New Ref).
